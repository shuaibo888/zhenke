# AGENTS.md

本文件面向在本仓库工作的 Codex（尤其是 Codex Cloud）。开始甄客行第一版或其他非简单修改前，必须依次完整阅读 `CODEX_CLOUD_GOAL.md` 和 `docs/项目交接文档.md`，再检查真实代码、路由、API 和数据契约。`CODEX_CLOUD_GOAL.md` 是当前全量重构的执行清单与完成门槛，不能只读本文件后直接编码。用户当前任务中的明确要求优先；若文档与代码现状不一致，先查明事实并在进度与结果中说明。

## 项目定位

甄客行是一个类似大众点评的综合本地生活平台，不是以商城为主体的项目。第一版以平台首页和“甄客帖”内容社区为主线，地点与真实定位是公共基础能力；现有商城作为辅助交易模块完整保留。

必须长期遵守以下产品边界：

- 首页和甄客帖优先，酒店、饭店、景区、商店等地点发现是主要心智；商城入口清楚、交易闭环可用，但不能重新占据首页主体。
- 甄客帖是登录用户围绕真实地点主动发布的本地生活内容；甄客验继续表示基于订单、试用、收货或核销资格形成的可信体验，两者不能混同。
- 第一版定位必须真实可用：设备定位、当前市区显示、地点选择、失败后的手动选择以及地图/导航都要正常。
- 第一版的“本地专区/外地专区”和首页内容不得按设备定位或帖子城市过滤。当前定位、帖子关联地点、专区内容分发是三个独立概念。
- 赛事等其他项目只能作为经过校验的轮播外链，不是甄客行内部业务模块。
- 第一版互动只包含评论、回复、“有用”和分享；收藏、关注、星级/分项评分、话题和个性化推荐不进入当前长任务。

## `yuanxing/` 原型规则

`yuanxing/` 是非技术人员借助 AI 制作的静态概念原型。它不是需求文档、架构设计、接口契约或正式代码来源；但它是甄客行用户端的**强制视觉基准**，不是可有可无的弱参考。发生冲突时，最新用户要求、`docs/项目交接文档.md`、现有真实业务契约和正规软件工程原则优先；在不冲突的视觉部分，必须保持与原型相近的整体色调、摄影卡片、圆角层级、留白节奏和移动端氛围。

- 开始第一版 UI 工作前必须检查原型的主要页面和手机/桌面表现，提炼设计 tokens 与组件清单；不得修改原型来冒充正式交付。
- 不复制原型的原生 HTML/全局 CSS/单体 `app.js` 到正式 React 工程，不导入原型的 Mock 数据、`localStorage` 状态、Hash 路由、模拟登录/支付/订单或硬编码演示图片选择器。
- 不把“燃赛”、赛事专题或“燃赛文体旅城市社区”做成甄客行主业务；赛事只允许作为后台轮播配置的外部链接。
- 不照搬原型按城市过滤帖子、含义模糊的“燃 / 游 / 商 / 我”导航、预置图片发帖、关注收藏、赛事套餐、区域运营、结算、数字人民币和区块链占位。
- 必须在 React/Ant Design 工程中重建设计系统：米白画布、陶土/暖橙主色、深棕文字、摄影内容、圆角卡片、柔和阴影和醒目发布入口；旧商城绿色只能保留为成功等语义状态，不得继续作为导航、按钮、标题或大面积背景的品牌色。
- 必须主动补齐加载、空态、失败、权限、登录回跳、删除态、分页、响应式、无障碍和安全校验。若原型做法不符合用户习惯、业务正确性、安全性或可维护性，舍弃其业务/交互做法，但仍要用正规的方式实现同一套视觉方向。

## 当前 Codex Cloud 第一版目标

下一次 Cloud 长任务以 `CODEX_CLOUD_GOAL.md` 和交接文档第 10 节为共同执行规格，目标是一次性交付可合并的甄客行第一版。这是用户端全站视觉与信息架构重构，加上甄客帖/地点/首页的真实全栈能力，不是只做分析、Mock、静态页面、一半接口或少数新增页面。至少完成：

- 真实甄客行首页、定位显示与降级、后台轮播、甄客帖列表/详情/发布/我的帖子/地点详情。
- 甄客帖独立后端表和接口、真实媒体、地点、可选商家、评论/回复、“有用”、分享、作者删除和超管删除闭环。
- 超管“甄客帖管理”和“首页轮播管理”，商家角色不可见且接口无权访问。
- 在 `delivery-backend/migrations/20260826_zhenkexing_v1.sql` 提交可人工执行的 MySQL 增量脚本；不要放进被忽略的 `delivery-backend/sql/`，不要由 Cloud 执行数据库迁移。
- 用户端主导航为“首页 / 甄客帖 / 商城 / 我的”；第一版推荐按时间倒序，本地/外地专区只按视角标签区分，任何帖子查询都不得按设备城市或地点城市过滤。
- 用户端现有商城、商品、商家、甄客验、购物车、结算、登录、个人中心、积分、优惠券、订单、试用和 404 等页面全部纳入甄客行视觉迁移；功能与历史路由保留，旧绿色商城外观不得原样残留。
- 商城只是甄客行的交易模块，不再有独立产品心智或第二套“我的”。第一版以稳定分类代码 `ZHENKE_HOTEL`、`ZHENKE_RESTAURANT`、`ZHENKE_SCENIC` 对应“甄客酒店 / 甄客饭店 / 甄客景区”，商家在现有商品体系上架，用户统一走商城下单、支付、线下核销码、商家核销和甄客验资格闭环；这些分类的订单必须由后端强制校验 `OFFLINE` 履约。
- 统一 `/profile` 按“内容创作 / 消费履约 / 权益资产 / 参与服务 / 设置安全”融合我的甄客帖、甄客验、订单与核销、优惠券、积分、试用和现有账号能力，不得把旧商城个人中心原样换色或另建商城个人中心。
- 保持现有 `shop_user`、`shop_merchant`、`sys_user`、商城、订单、支付、退款、试用、核销、甄客验和历史路由兼容。

Cloud 必须在任务分支维护 `CODEX_CLOUD_PROGRESS.md`，逐阶段记录页面覆盖、后端范围、实际改动、验证和剩余问题。只新增几页、只换主题色、只通过构建、或仍有未解释的页面未迁移，都不能宣告第一版完成。不得用“先做一个简版”“其余后续优化”缩小已明确的当前范围。

Cloud 必须有产品思维：逐页确认核心用户任务、第一屏信息、主要操作、内容到交易的衔接以及手机/桌面的不同布局，不能机械照抄原型，也不能机械保留旧商城页面结构。常规的信息分组、卡片形态、文案和交互取舍由 Cloud 在既定业务边界内自主完成并记录，不要为这些事项暂停等待审批。

在交接文档已经给出默认答案的事项上直接作出工程实现，不因组件命名、样式间距、字段长度、索引、VO 拆分、错误文案或测试组织暂停。缺少真实地图密钥时完成适配、配置、失败降级和 mock 测试，不伪造运行成功。只有生产操作、真实密钥/外部账号授权、执行迁移、不可逆数据变更、收费服务或实质性需求冲突才需要等待用户。

## 仓库结构与技术基线

- `delivery-frontend/`：用户端，React 19、Umi 4、Ant Design 6、TypeScript。
- `delivery-admin-frontend/`：管理/商家端，同一前端技术栈，正式基路径为 `/admin/`。
- `delivery-backend/`：Spring Boot 3.5、Java 17、Maven 多模块单体；商城与甄客行业务主要位于 `ruoyi-shop`。
- `docs/项目交接文档.md`：当前产品方向、业务边界、第一版默认决策、Cloud 长任务范围和验收标准的权威交接文档。
- `zhenke/`、`.deploy/`：本地或生产部署材料，默认不提交，也不得在普通 Cloud 开发任务中操作。

不要恢复旧的 `ruoyi-ui`，不要把项目拆成微服务，不要把演示目录、Mock 页面或本地数组当作正式实现。前端只展示真实 API 与持久化结果。

## Codex Cloud 环境

Cloud 环境固定使用 Node.js 20 和 Java 17。首次创建环境执行：

```bash
bash scripts/codex-cloud-setup.sh
```

缓存恢复后的维护脚本执行：

```bash
bash scripts/codex-cloud-maintenance.sh
```

两份脚本只安装锁定依赖并预取 Maven 依赖，不启动服务、不访问生产数据库，也不执行数据库迁移。

`ruoyi-admin` 的 PCC Starter 来自独立私有仓库，只负责运行时加载私密配置。普通本地和生产 Maven 构建默认包含它；Codex Cloud 没有生产配置和密钥，因此所有 Cloud Maven 命令必须增加 `-Dcodex.cloud=true`，仅在云端验证中排除该运行时依赖。不要把这个参数用于生产打包，也不要因此删除或复制 PCC 源码。

## 常用验证命令

根据改动范围选择最小但充分的验证；用户明确要求“只改代码、不运行测试”时服从该要求并在结果中说明未验证项。

```bash
# 用户端
npm --prefix delivery-frontend run typecheck
npm --prefix delivery-frontend run build

# 管理端
npm --prefix delivery-admin-frontend run typecheck
npm --prefix delivery-admin-frontend run build

# 后端商城模块及其依赖（Cloud）
mvn -f delivery-backend/pom.xml -Dcodex.cloud=true -pl ruoyi-shop -am test

# 后端完整打包（Cloud 验证产物，不得用于生产部署）
mvn -f delivery-backend/pom.xml -Dcodex.cloud=true clean package

# 通用静态检查
git diff --check
```

两个前端当前都没有 `test` 脚本，不要运行不存在的 `npm test`。构建成功、静态检查成功、运行时成功、数据库迁移成功和真实业务闭环成功是不同证据，不得互相替代。

## 实现约束

- 新页面使用正式独立路由和页面文件，保证刷新、分享、登录回跳、前进后退可用；移动端用户页默认不增加左侧栏。
- 后端保持 `Controller -> Service -> Mapper/XML -> domain/dto/vo` 分层。身份、事务、资格和状态机在 Service 校验；资源归属、期望原状态和并发条件同时落实到 Mapper SQL。
- 前端提交的用户/商家身份、价格、库存、订单状态、报告作者、审核状态、地点名称和坐标都不可信，必须由后端重新确认。
- API 业务成功以响应体 `code === 200` 为准，不能只看 HTTP 200；列表通常为 `{ rows, total }`。
- 数据库改动只新增版本化增量脚本，不改写已执行的历史迁移。当前第一版迁移固定提交到 `delivery-backend/migrations/20260826_zhenkexing_v1.sql`；未经当前任务明确授权，不执行迁移、不连接生产库。
- 不破坏既有商城的订单、支付回调、退款、试用、核销、甄客验和历史分享链接闭环。
- 定位数据、媒体、用户资料和联系方式按最小权限处理；不记录与业务无关的持续位置轨迹。
- 不提交 `.env`、真实密钥、数据库快照、日志、`node_modules`、`dist`、`target` 或部署制品。

## 长任务工作方式

1. 先完整阅读 `CODEX_CLOUD_GOAL.md`、交接文档、相关代码、全部用户端路由/API 和数据模型，再形成覆盖数据库、后端、用户端全站、管理端和验证的实施计划。
2. 工作区已有改动均视为用户有效改动；不要清理、重置、覆盖或混入无关提交。
3. 从仓库中已预置的 `CODEX_CLOUD_PROGRESS.md` 开始持续更新，按目标文件阶段 A-G 记录覆盖矩阵、改动、命令、证据和剩余项；不得删除它来绕过未完成清单。每完成一个可独立验证的领域形成一次清晰提交，避免到最后才得到不可审查的大块改动。
4. 必须先建立设计系统和全局壳，再完成甄客帖/地点全栈闭环，再逐页迁移全部既有用户页面，最后完成管理端、回归和视觉验收。不得只改首页和新页面后提前结束。
5. 在已确认范围内持续推进实现和静态/构建/浏览器验证。遇到可通过代码、交接文档与仓库上下文解决的问题，不要因用户暂时离线而停止，也不要把普通工程取舍转换为审批问题。
6. 只有涉及产品规则缺失、生产操作、外部账号授权、真实密钥、不可逆数据变更或明显扩大范围时才暂停并请求用户决定；若平台执行被硬性中断，保留准确进度与已验证提交，供下一次从首个未完成阶段继续。
7. 只有 `CODEX_CLOUD_GOAL.md` 的完成定义全部满足才可宣告完成。耗时长短和改动文件数量不是目标，但十分钟式的少量页面/换色提交、未迁移页面、未完成后端闭环或没有证据的结果一律视为未完成。
8. 完成后汇报实际改动、全站页面覆盖、验证与截图证据、未验证项和风险。不要声称已部署、已迁移或已通过真机/真实支付验证，除非确有对应证据。

## Git 规则

- 仅暂存本任务已检查的文件，不使用 `git add -A`。
- 不使用 `git reset --hard`、`git checkout --` 等方式丢弃用户改动。
- 默认不部署、不创建或合并 PR、不推送生产分支，除非用户在当前任务中明确要求。
- Cloud 任务需要交付提交或 PR 时，提交信息应准确描述范围，并在提交前至少执行 `git diff --check` 及与改动相符的验证。
- 当前第一版长任务完成后应提交并推送 Cloud 任务分支；平台支持时创建 PR，但不得自动合并主分支或部署生产。最终说明必须区分已通过的代码验证、尚未执行的 SQL、缺少密钥的第三方联调和需要用户进行的真实环境验收。
