# Codex Cloud：甄客行第一版全量重构目标

> 本文件是 Codex Cloud 长任务的执行清单和完成门槛。开始任务前必须依次完整阅读 `AGENTS.md`、本文件、`docs/项目交接文档.md`，并检查真实代码、路由、接口、数据模型与 `yuanxing/`。本文尚未完成的阶段不能在聊天中用一句“后续再做”跳过。

## 1. 任务性质

这不是“给现有商城增加几个甄客帖页面”，而是以现有商城的账号、商家、交易、履约、甄客验和后台权限为可靠底座，把用户端整体重构为“甄客行”综合本地生活平台，同时新增完整的地点、甄客帖、首页运营和超管管理能力。

预期会修改大量用户端页面、公共组件、全局样式、路由、API、后端分层代码、Mapper/XML、权限、管理端页面、测试和增量 SQL。不得为了控制改动文件数量而保留不符合甄客行定位的旧页面；也不得为了制造大改动而无意义重写稳定的支付、订单状态机或身份逻辑。是否完成只看业务闭环、全站覆盖和验证证据，不按耗时或文件数量判断。

Cloud 需要主动承担产品设计责任：逐页判断用户来这里要完成什么、第一屏应看到什么、主要操作如何到达、内容与交易如何衔接、移动端和桌面端怎样分别组织。不能机械照抄原型，也不能机械保留旧商城布局；应在本文边界内采用更符合普通用户心智的正规方案，并把重要取舍记录在 `CODEX_CLOUD_PROGRESS.md`，不因卡片顺序、分组、文案、组件形态等常规产品判断等待用户审批。

## 2. 原型的双重边界

`yuanxing/` 是强制视觉参照，不是业务或技术参照。

- **视觉上必须接近**：正式用户端应继承原型的暖色本地生活气质、米白画布、陶土/暖橙主色、深棕文字、摄影内容、圆角卡片、柔和阴影、紧凑移动端信息层级、醒目发布入口和底部导航节奏。页面不能继续看起来像换了名称的绿色商城。
- **工程上不得复制**：不得复制原型的原生 HTML、全局 CSS、单体脚本、Hash 路由、Mock 数据、`localStorage` 状态、模拟登录/支付/订单或硬编码图片选择器。必须在现有 React/Umi/Ant Design 工程内重建可维护组件和真实 API 闭环。
- **业务上不得照搬**：赛事主导、城市过滤、含义模糊的导航、关注收藏、赛事套餐、区域运营、数字人民币和区块链占位均不是本次需求。赛事只能作为经过后台校验的轮播外链。
- **冲突处理**：业务正确性、安全性、真实数据契约和可用性高于视觉相似；在不冲突的视觉部分，原型是必须执行的设计方向，而不是可做可不做的建议。

建议从原型提炼并集中维护设计变量，基准方向如下；可以为对比度和组件状态微调，但不得退回旧商城绿色品牌体系：

```text
canvas/background  #f5f2ec
surface            #fffdf8
primary/terracotta #8a3f32
primary-dark       #5e2d26
action-orange      #ff6b1a
gold-accent        #b7791f
ink                #1e2924
muted              #65716b
line               #dedbd2
success-green      #27704b（仅用于成功状态，不得作为主品牌色）
```

## 3. 用户端全站覆盖矩阵

Cloud 开始编码前要对照当前 `.umirc.ts` 和 `src/pages` 更新本表；完成时逐项核对。除纯跳转回调外，所有既有用户页面都必须迁移到统一甄客行视觉系统。所谓“保留商城功能”是保留真实功能和数据，不是保留旧商城视觉壳。

| 领域 | 路由/页面 | 本次要求 |
| --- | --- | --- |
| 全局壳 | `AppChrome`、全局头部、移动端底栏、桌面容器、反馈状态 | 全量重构为甄客行壳；主导航固定“首页 / 甄客帖 / 商城 / 我的” |
| 首页 | `/` | 全量替换为甄客行首页；真实定位、轮播、本地生活入口、甄客帖内容为主体，商城只保留辅助入口 |
| 甄客帖 | `/posts`、`/posts/:postId`、`/posts/publish`、`/profile/posts` | 新增正式独立路由和真实业务闭环，不能是静态页面或 Mock |
| 地点 | 地点搜索/选择、`/places/:placeId`、地图/导航 | 新增真实能力与失败降级；地点必选、商家可选，两者分开保存 |
| 商城 | `/mall` | 作为甄客行内的交易模块重做；首屏突出“甄客酒店 / 甄客饭店 / 甄客景区”三大场景，保留动态分类、搜索、商品流和购物车，不再使用旧商城左侧分类栏式心智 |
| 商品 | `/products/:productId` | 保留商品、试用、购买等真实能力；按酒店/饭店/景区等本地生活场景组织套餐信息、商家、使用规则和到店核销说明，重写展示层和移动端布局 |
| 商家 | `/merchants/:merchantId` | 保留已入驻商家信息和商品，重写为本地生活语境；不得把普通地点冒充商家 |
| 甄客验 | `/reports/:reportId`、用户甄客验入口 | 保留可信体验语义和历史分享链接，重写视觉并显著区别甄客帖 |
| 结算 | `/checkout`、`/checkout/success`、购物车抽屉 | 保留价格、库存、支付、回跳和状态逻辑，重写视觉与响应式，不得弱化安全校验 |
| 登录 | `/auth` | 保留现有登录与目标页回跳，迁移品牌、版式、表单状态和移动端体验 |
| 我的 | `/profile` | 只保留一个甄客行个人中心；融合现有商城“我的”，继续提供订单、积分、优惠券、试用、甄客验，并新增我的甄客帖，不能再出现独立的“商城我的” |
| 用户资产 | 积分、积分记录、优惠券、优惠券详情 | 全部迁移到统一视觉体系，保留真实数据和历史行为 |
| 订单 | 订单列表、订单详情 | 全部迁移，保留支付、退款、核销等既有状态和操作 |
| 试用 | 试用列表、试用详情 | 全部迁移，保留现有资格、详情和直达路由 |
| 技术页 | `404`、SSO callback | 404 迁移品牌；SSO callback 以行为正确为主，不要求复杂视觉 |

用户端视觉迁移不是在旧 `commerce.less` 末尾追加几条覆盖规则。应先建立甄客行设计 tokens、基础布局和共享组件，再逐页迁移并清理对应旧样式。绿色只能用于成功/安全等语义状态；大面积绿色导航、按钮、标题、渐变或商城主视觉均视为未完成。

### 3.1 商城作为甄客行模块的首版产品结构

商城不是另一个平级产品，也不能有自己的第二套首页、主导航、用户身份或个人中心。用户从甄客行首页、地点/商家详情、底部“商城”进入交易模块，购买后仍回到统一“我的”查看订单、核销码、优惠券、积分、试用和甄客验。

- “甄客酒店”“甄客饭店”“甄客景区”是第一版三个平台重点商品分类和场景入口，不是三套独立预订系统。商家入驻并审核通过后，可以在现有商品管理中选择这些分类上架商品，例如酒店套餐、餐饮套餐/代金券、景区门票或“长城一日游”。
- 三类商品第一版统一走现有商城订单、支付和线下核销闭环：商家上架 -> 用户购买并支付 -> 订单生成核销码 -> 用户到店/现场出示 -> 对应商家预览并确认核销 -> 订单进入已完成/已核销状态 -> 用户按既有资格发布甄客验。不得另外做一套假订单或只在前端显示“核销成功”。
- 三个分类使用稳定的分类代码 `ZHENKE_HOTEL`、`ZHENKE_RESTAURANT`、`ZHENKE_SCENIC`，展示名称可由超管通过现有分类管理维护；增量 SQL 以幂等方式补齐这三项，不覆盖或删除已有分类和商品关系，前端不得硬编码数据库自增 ID。
- 这三个分类的商品必须支持并实际使用 `OFFLINE` 履约。商家保存/上架、用户结算和服务端创建订单时都要校验，不能信任前端选择，也不能让此类商品走普通快递流程。其他既有分类及其线上配送能力继续保留。
- 第一版复用通用商品/套餐模型和库存，不自动扩张为房态日历、桌位预订、分时票、实名制游客、旅游团排期或多规格 SKU 系统。为了让本地生活商品可真实售卖，应在现有商品模型上补齐通用的套餐内容、使用须知、有效期说明、是否需预约、预约说明、退款/过期规则、适用地点/商家展示等必要字段；具体字段拆分由 Cloud 根据现有数据模型正规设计并落实到 SQL、后端、商家端和用户端，不能只塞进图片或前端常量。
- 商城首页要用甄客行设计语言展示三大场景入口、正在售卖的真实商品和其他动态分类。三大场景无商品时展示真实空态与商家入驻提示，不填充假酒店、假景区或假饭店数据。

### 3.2 统一“我的”信息架构

甄客行只有一个底部“我的”和一个 `/profile`。建议按用户任务而不是按旧系统模块堆菜单：

- 顶部：统一 `shop_user` 头像、昵称、账号安全与资料入口。
- 内容创作：我的甄客帖、我的甄客验，清楚区分自由发布与消费后可信体验。
- 消费履约：我的订单为主入口，优先呈现“待付款 / 待使用（含线下核销码）/ 待收货 / 售后 / 已完成”等用户可理解的状态；线上配送订单和线下核销订单在同一订单体系内以履约类型区分。
- 权益资产：优惠券、积分和积分记录。
- 参与服务：我的试用；地址管理只在配送业务需要时出现，不应占据本地生活用户的首要位置。
- 设置与安全：编辑资料、账号名/密码、安全和退出登录。

移动端首屏应优先展示身份、内容入口、待办订单/核销入口和常用权益，详细设置可以下沉。不得把旧个人中心原样换色，也不得再造一套只属于商城的用户资产页。

## 4. 新业务与后端范围

完整实现以下真实持久化能力，详细字段、权限、接口和安全边界以交接文档第 2、4、6、7、10 节为准：

- 版本化 MySQL 增量脚本 `delivery-backend/migrations/20260826_zhenkexing_v1.sql`，包含地点、甄客帖、媒体、评论/回复、“有用”、首页轮播、菜单和权限；Cloud 只编写和检查，不执行迁移。
- 同一增量脚本幂等补齐三个稳定商品分类，并为本地生活套餐必要字段提供安全增量；不得覆盖既有分类代码、商品、订单或生产数据。
- `ruoyi-shop` 中完整的 `Controller -> Service -> Mapper/XML -> domain/dto/vo` 实现和相称测试。
- 甄客帖公开列表/详情、发布、真实媒体、地点、可选商家、我的帖子、作者逻辑删除、评论/回复/删除、“有用”、分享所需数据。
- 首页聚合、轮播、定位/逆地址、POI 搜索、地点详情和导航适配；缺少地图密钥时完成配置、明确降级和 mock 测试，不能伪造成功。
- 超管甄客帖管理和首页轮播完整 CRUD/启停/排序；商家菜单不可见、路由不可达、接口无权访问。
- 保持 `shop_user`、`shop_merchant`、`sys_user`、商城、订单、支付、退款、试用、核销、甄客验和历史链接兼容。

## 5. 强制执行阶段

Cloud 必须从仓库中预置的 `CODEX_CLOUD_PROGRESS.md` 开始逐阶段维护，写明已检查范围、实际改动、验证命令、结果和剩余问题。它是本次 PR 的一部分，不得删除重建来绕过清单，也不得只在聊天消息中报告进度。

### 阶段 A：基线盘点与设计审计

- 盘点用户端所有路由、页面、共享组件、API 和旧绿色样式来源。
- 盘点后端用户、商家、甄客验、上传、评论、“有用”、地图、权限和删除实现。
- 查看 `yuanxing/` 的手机与桌面布局，提炼可执行视觉 tokens 和组件清单。
- 在进度文件中记录页面覆盖矩阵和不可破坏的业务链路。

### 阶段 B：设计系统与全局壳

- 建立统一 tokens、字体层级、间距、圆角、阴影、按钮、卡片、表单、媒体、状态和响应式规则。
- 重构 `AppChrome`、首页框架、移动端底栏、桌面容器、加载/空态/错误态。
- 清理旧商城品牌色在主壳中的传播，并保证已有路由仍可访问。

### 阶段 C：数据库与后端闭环

- 完成增量 SQL、领域模型、DTO/VO、Mapper/XML、Service、Controller、权限与测试。
- 服务端重新校验身份、地点、商家、媒体、资源归属、状态和并发条件。
- 完成删除后列表/详情/分享不可见、评论级联隐藏及商家无权访问等边界。

### 阶段 D：首页、甄客帖与地点

- 完成首页、列表、详情、发布、我的帖子、地点选择/详情、真实定位和降级。
- 完成评论/回复、“有用”、分享、作者删除和登录回跳。
- 移动端优先，同时完成合理的桌面版，而不是简单把手机页面拉宽。

### 阶段 E：既有商城与个人中心全量视觉迁移

- 按第 3 节矩阵逐页迁移商城、商品、商家、甄客验、购物车、结算、登录、个人中心、资产、订单、试用和 404。
- 把三大商品分类、商家上架、用户购买、支付后核销码、商家核销和甄客验资格串成真实验收链路；统一“我的”承接内容、订单、核销和资产，不保留商城孤岛。
- 保留真实业务逻辑、API、历史直达路由和状态机，统一为甄客行视觉与信息架构。
- 每个未修改页面都必须在进度文件中说明为什么不需要修改，并给出它已经满足新视觉系统的证据；不能默认为“旧功能不动”。

### 阶段 F：超管、回归与视觉验收

- 完成超管两个管理模块及权限回归。
- 执行用户端/管理端 typecheck 和 build、后端测试、`git diff --check`。
- 可启动页面时，对关键路径至少做 `390x844` 移动端和 `1440x900` 桌面端截图检查：首页、帖子列表、帖子详情、发布、地点详情、商城、商品详情、结算、个人中心、订单详情、甄客验详情、超管甄客帖、超管轮播。
- 检查控制台错误、窄屏溢出、固定底栏遮挡、无数据/失败/删除状态、登录回跳和直接刷新。

### 阶段 G：最终审计与 Git 交付

- 对照本文件和交接文档逐项核对，不得把未完成项悄悄改写为“后续优化”。
- 形成按领域清晰的阶段提交并推送 Cloud 任务分支；平台支持时创建 PR，不自动合并、不部署生产。
- PR 说明列出页面覆盖、数据库/后端/用户端/管理端改动、实际验证、截图证据、未执行 SQL、缺少真实密钥的联调项和用户验收步骤。

## 6. 禁止提前宣告完成

出现以下任一情况，任务都不能标记完成：

- 只新增首页或甄客帖页面，而商城、登录、个人中心、订单、试用、甄客验等仍是旧绿色商城视觉。
- 只换 Logo、导航、主色或在旧 CSS 后追加覆盖层，页面结构、信息层级和交互没有完成系统性迁移。
- 前端使用 Mock、本地数组、假上传、假定位、假发布或硬编码详情代替真实 API 与持久化。
- 后端只有表或 Controller 骨架，没有 Service 校验、Mapper 条件、权限、删除语义和测试。
- 只通过 typecheck/build 就宣称业务完成，未核对页面矩阵、关键状态、测试和视觉证据。
- 未创建或未完整更新 `CODEX_CLOUD_PROGRESS.md`，或其中仍存在未解释的待办、失败和跳过项。
- 为了赶快交付而删除、绕过或弱化现有支付、订单、退款、试用、核销、甄客验、安全和权限规则。

如果 Cloud 单次执行因平台硬限制被中断，应保留已经验证的阶段提交和准确的进度文件；下一次必须从首个未完成阶段继续，不能重新包装已完成的小部分并宣称整个第一版完成。

## 7. 最终完成定义

只有同时满足以下条件才可结束长任务：

1. 第 3 节所有页面已实现或有充分、可核验的不改理由，用户端不再呈现旧绿色商城主视觉。
2. 首页、甄客帖、地点、定位、互动、作者删除、超管删除和轮播形成真实全栈闭环。
3. 现有商城和用户资产功能完整保留，并已迁移进统一甄客行视觉系统。
4. SQL 已提交但未执行；权限、敏感信息、媒体和定位边界符合交接文档。
5. 可执行验证已经运行并记录；无法进行的真实数据库、地图、微信、支付和生产验收被明确标注，未伪造结果。
6. `CODEX_CLOUD_PROGRESS.md`、阶段提交和 PR/最终报告提供足够证据，使用户第二天可以直接拉取、执行 SQL、配置密钥并开始真实验收。

## 8. 提交给 Codex Cloud 的启动指令

用户创建新的 Cloud 任务时可直接发送下面这段，不需要再复制本文全部内容：

```text
完成“甄客行第一版全量重构”目标。这不是给商城增加几个页面，也不是十分钟式简版。开始前必须完整阅读并严格执行 AGENTS.md、CODEX_CLOUD_GOAL.md、CODEX_CLOUD_PROGRESS.md、docs/项目交接文档.md，然后检查真实代码和只读的 yuanxing/。在既定边界内发挥产品思维，按进度文件 A-G 持续实现数据库增量 SQL、后端、用户端全站重构、商城三大场景、统一“我的”、管理端、测试和双端视觉验收。不得只换色、只改首页、使用 Mock、保留旧绿色商城孤岛或把未完成项写成后续优化；不得在进度清单全部完成且有实际证据前宣告完成。普通产品和工程取舍自主决定，不等待我审批；只有 AGENTS.md 明确列出的真实阻塞才暂停。完成后按领域提交并推送 Cloud 任务分支，平台支持时创建 PR，不执行 SQL、不部署、不自动合并。
```
