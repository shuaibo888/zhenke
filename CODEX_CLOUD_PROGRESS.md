# 甄客行第一版 Cloud 执行进度

> 初始状态：未开始。执行本任务的 Codex Cloud 必须持续更新本文件，保留真实事实，不得预先勾选、批量勾选或把未完成项改名为“后续优化”。每个完成项至少写出主要实现文件和对应验证证据；无法验证时明确写出原因和影响。

## 0. 任务与基线

- Cloud 原任务分支：`work`；GitHub PR 源分支：`origin/codex`；本地验收修复分支：`codex/cloud-zhenkexing-v1`
- 起始提交：`264a23d17a8a010fa9076dca41a08860f5772c8a`
- 工作区初始状态：`干净；git status --short 无输出`
- 最后更新时间：`2026-08-27`
- 当前阶段：`代码审计与 PR 交付完成；转由用户执行本地 SQL、真实接口和业务调试`
- 阻塞项：`无代码交付阻塞`

## A. 基线、产品与视觉审计

- [x] 完整阅读 `AGENTS.md`、`CODEX_CLOUD_GOAL.md`、`CODEX_CLOUD_PROGRESS.md`、`docs/项目交接文档.md`。
- [x] 盘点全部用户端路由、页面、共享组件、API 和全局样式。
- [x] 盘点账号、商家、商品分类、订单/支付/退款、线下核销、试用、甄客验、媒体、地图、分享与权限实现。
- [x] 检查 `yuanxing/` 手机与桌面主要页面，记录可采用的视觉 tokens、组件和布局节奏。
- [x] 记录不可破坏的历史路由、状态机、身份归属和安全边界。

证据与产品决策：

已盘点真实 `.umirc.ts`、全部 `src/pages`/services、`ruoyi-shop` Controller/Service/Mapper/XML、RBAC、交易/核销/甄客验和只读 `yuanxing/` HTML/CSS/主要资产。原型提炼为米白画布、陶土主色、暖橙动作色、摄影卡片、20px 左右圆角、柔和阴影、手机双列内容卡和四项底栏；未复制原型脚本、Mock 或业务。

已确认消费者、商家和后台操作员分别继续使用 `shop_user`、`shop_merchant`、`sys_user`；甄客帖地点必选、商家可选且不代表到访/消费认证。2026-08-31 最新口径取消本地/外地专区，帖子流改为三种身份筛选；身份城市来自发布地点。城市内容范围新增后端参数开关，默认全平台，开启后甄客帖与甄必享同时按当前市过滤。起始分支和工作区通过 `git status --short --branch`、`git rev-parse HEAD` 检查。已完整读取四份规格文件；真实代码和原型审计继续进行。

## B. 设计系统与全局应用壳

- [x] 建立暖色甄客行设计 tokens，绿色仅作语义状态色。
- [x] 结构性重写 `AppChrome`、桌面头部、移动端顶栏/四项底栏、发布入口和桌面内容容器。
- [x] 逐项完成并验证统一按钮、卡片、表单、媒体、弹窗、加载、空态、错误态和删除态在全部旧页面中的落地。
- [x] 确认全部既有路由仍可直达、刷新、前进后退和登录回跳。

实现与证据：

2026-08-26 复审发现上一轮仅用 2 行压缩 CSS、单行页面和批量颜色替换，不能证明全站视觉与信息架构重构，原“完成”结论无效。本轮把 `src/styles/zhenke.less` 重建为可审查的完整设计系统，新增页面状态、任务标题、套餐规则、结算旅程和甄客验资格组件，重写 `AppChrome.tsx`，并逐页落地到全部正式路由。Playwright 双视口矩阵确认关键路由无横向溢出和控制台错误；登录回跳、详情沉浸底栏和结算固定操作相互不再遮挡。

## C. 数据库、后端与权限

- [x] 保持历史 `delivery-backend/migrations/20260826_zhenkexing_v1.sql` 不变；完成并静态检查后续 `delivery-backend/migrations/20260828_zhenkexing_v2.sql`，未执行迁移。
- [x] 完成地点、甄客帖、媒体、评论/回复、“有用”和首页轮播全套分层代码。
- [x] 幂等补齐三大稳定商品分类和本地生活套餐必要字段。
- [x] 后端强制三大分类 `OFFLINE` 履约并保留真实订单/核销/甄客验资格链路。
- [x] 完成作者删除、超管删除、商家无权访问和删除后不可见等权限/状态边界。
- [x] 增加与改动相称的后端测试。

实现与证据：

历史增量脚本 `delivery-backend/migrations/20260826_zhenkexing_v1.sql` 包含地点、帖子、媒体、上传归属、评论、有用和轮播 7 张新表、索引、三类幂等分类、套餐字段与仅超管权限；该脚本可能已经执行，现保持不可变。后续 `delivery-backend/migrations/20260828_zhenkexing_v2.sql` 单独增加超管甄必享官方内容、用户点赞评论与无限库存。`ShopZhenke*` 分层完成帖子、地点、媒体、评论/回复、有用、删除、轮播和超管闭环；`ShopZhenkeEnjoy*` 使用独立表和接口完成官方内容发布/下线/删除、四类公开列表、详情、点赞及评论回复，甄客帖不再包含任何甄必享归类。发布地点仍由服务端按腾讯 provider id 二次查询，前端名称与坐标不作为可信来源；上传记录按用户、类型、24 小时有效期和一次性帖子认领原子校验。甄客验上传也已按 `shop_user` 隔离目录，发布时重新验证资源属于当前账号归属、类型与服务端真实文件，并在报告或任一媒体写入失败时让事务整体失败，避免无图半成品报告。商品保存与订单创建均按稳定分类代码强制 OFFLINE，需要预约时服务端额外强制预约说明。

## D. 首页、甄客帖与地点

- [x] `/` 甄客行首页：真实定位、降级、轮播、入口和甄客帖主体内容。
- [x] `/posts`：三种身份筛选；城市开关关闭时为全平台内容，开启后按当前市过滤。
- [x] `/posts/:postId`：媒体、作者、视角、地点、可选商家、评论/回复、“有用”和分享。
- [x] `/posts/publish`：登录回跳、真实上传、标题/正文/建议/视角/地点/商家和防重复提交；不含甄必享归类。
- [x] `/profile/posts`：真实我的帖子与作者删除。
- [x] `/places/:placeId`：地点详情、关联帖子、地图/导航与失败降级。

实现与证据：

新增正式路由 `/posts`、`/posts/:postId`、`/posts/publish`、`/profile/posts`、`/places/:placeId`；`services/zhenke.ts` 只访问真实 API，认证写操作统一附带正式 token，已修复曾导致 `/api/api/...` 的重复前缀。首页启动真实浏览器定位 + 后端逆地址，并提供失败后的手动市区显示；当前城市会无感随公共内容列表请求提交，后端仅在运营开关开启时用于甄客帖与甄必享过滤。发布使用真实上传、provider 地点和可选商家，并用同步预占避免并发选择突破 9 个媒体/1 个视频限制；详情含评论/回复、有用、微信分享卡片、浏览器分享、作者删除、地点和商家语义区分。公共查询不会展示停用地点，也不会暴露未通过审核、已停用或已删除的关联商家；超管仍可查看历史关联用于治理。我的帖子支持完整分页，已删除卡片不可再进入已失效详情。

## E. 商城、酒店、景区、饭店四大营业分类与统一“我的”

逐页填写主要改动文件；“未改”必须给出已经满足新设计系统的可核验证据。

- [x] `/mall`：商城/酒店/景区/饭店四个大类并列；商城完整聚合原有动态分类，三个新增大类按稳定分类查询；支持搜索、真实商品和空态。
- [x] `/products/:productId`：本地生活套餐信息、商家地点、规则、线下核销和既有购买/试用能力。
- [x] `/merchants/:merchantId`：甄客行语境的入驻商家与商品展示。
- [x] 购物车抽屉、`/checkout`、`/checkout/success`：统一视觉且不破坏价格、库存、优惠和支付。
- [x] `/auth`：统一品牌、表单状态和目标页回跳。
- [x] `/profile`：唯一甄客行“我的”，融合内容、订单/核销、权益、试用和设置。
- [x] `/profile/points`、`/profile/point-records`。
- [x] `/profile/coupons`、`/profile/coupons/:userCouponId`。
- [x] `/profile/orders`、`/profile/orders/:orderId`，清楚区分待使用核销与配送履约。
- [x] `/profile/trials`、`/profile/trials/:applicationId`。
- [x] `/profile/reports`、`/reports/:reportId`，保留甄客验可信语义与历史分享。
- [x] `404`；SSO callback 行为回归。
- [x] 商家商品管理已支持三大分类、套餐字段和线下核销约束；新建商品不预填售价，库存默认无限且可切换有限库存。

实现与证据：

商城交易页现在按“商城 / 酒店 / 景区 / 饭店”四个大类组织真实商品。“商城”通过服务端分页条件聚合并只展示原有非本地生活动态分类，继续承接 main 原有自营商品、试用、线上购买、支付、发货物流、已有线下核销、退款和甄客验等完整能力；酒店、景区、饭店按三个稳定分类查询并由服务端强制使用线下核销。原商城的试用/推荐甄客验发现流已恢复到 `/mall`，随当前大类、动态分类和关键词进行服务端分页筛选；“推荐”文案同步改为真实存在的商城内容流，线下订单评分使用“核销体验”而不是“物流服务”。商品页新增场景套餐、预约、有效期、退款/过期、入驻商家地址与导航；结算页明确“确认订单 / 安全支付 / 到店使用或配送”并正确处理购物车混合履约拆单；支付成功页按 OFFLINE 显示核销任务。统一订单把 PAID+OFFLINE 表述为“待使用”，核销后表述为“已核销”。甄客验详情新增资格来源带，与甄客帖在视觉和文案上明确区分。积分、优惠券、订单、试用、甄客验和账号安全均使用统一任务页头及暖色体系。管理端商品表单按稳定分类代码显示并强制填写套餐字段，服务端仍作最终校验。

## F. 超管、回归与视觉验收

- [x] 超管甄客帖管理：查询、筛选、详情、媒体预览和逻辑删除；商家不可见/不可达/无权限。
- [x] 超管首页轮播管理：完整 CRUD、上传、跳转校验、排序、有效期和启停。
- [x] 用户端最终 typecheck 与 build。
- [x] 管理端 typecheck 与 build。
- [x] 后端 `ruoyi-shop -am test`。
- [x] `git diff --check`。
- [ ] `390x844` 全部关键页面视觉与交互检查（Cloud 留有执行记录，但临时截图不可追溯；本地复核受桌面安全策略阻止）。
- [ ] `1440x900` 全部关键页面视觉与交互检查（同上）。
- [ ] 控制台、溢出、底栏遮挡、空/错/删状态、刷新、回跳和直接链接的本地完整复核。

命令、结果与截图路径/说明：

- 用户端 typecheck、build：本轮重写后重新执行并通过。
- 管理端 typecheck、build：通过。
- 后端 `ruoyi-shop -am test`：最新复跑 81 tests，通过，0 failures / 0 errors / 0 skipped。
- `git diff --check`：通过。
- Cloud 记录其曾在真实 Umi dev server 上对首页、帖子列表/详情/发布、地点、商城、商品、结算、支付成功、统一我的、订单详情和甄客验详情逐页执行 `390x844` 与 `1440x900`，并对超管帖子、轮播执行双视口；其记录称逐页断言 `scrollWidth <= clientWidth` 且无 console error，截图位于 Cloud 临时路径 `/tmp/zhenke-visual-v3/matrix-*.png`、`admin-*.png`。这些临时产物未提交且当前不可追溯，因此只作为历史执行记录，不作为本地最终验收证据。
- 视觉检查使用 Playwright 路由级代表数据仅用于渲染覆盖，没有写入仓库、没有替代业务测试或冒充数据库闭环。真实 API 失败态也在不拦截目标远端请求时检查；当前容器没有执行 SQL、没有数据库/真实账号/微信支付/地图密钥，因此真实发布、支付、核销、地图和超管权限仍必须在用户执行迁移并配置环境后验收。

2026-08-27 PR 本地验收修复：

- 修复管理端本地生活套餐字段只回显、不进入新增/修改请求体的问题，三类套餐现在提交内容、须知、有效期、预约和退款规则，后端继续强制完整性及 `OFFLINE`。
- 首页帖子与轮播改为独立失败边界；轮播失败不再隐藏帖子。当前定位仅保存于浏览器会话，不形成位置轨迹，并可在发帖选点时选择当前市区优先或全国搜索。
- 地点导航复用微信/系统地图能力，桌面兜底改由后端生成；新增 `TENCENT_MAP_REFERER`，不再把服务端地图 Key 当作 URI API 应用名称或写入前端。
- 超管甄客帖管理补齐关联商家、发布时间筛选和评论/有用计数，主导航按真实权限隐藏甄客帖与轮播入口；服务端验证搜索长度、商家 ID、时间范围。
- 后端补齐未登录发布、仅视频、无效商家关联、有用并发状态、回复根节点、评论归属/级联、作者/超管删除和筛选边界测试，并对帖子、媒体和评论写入结果增加事务内校验。
- 第二轮验收补齐超管列表/详情/删除的独立权限，前端按钮与直达路由同步按权限收口；轮播新增、修改、启停和删除也分别按权限显示和校验。
- 商家核销现在同时写入受状态约束的订单状态日志，明确拒绝重复核销、其他商家核销和线上配送订单；新增测试证明线下核销后具备发布甄客验资格。
- 用户端把帖子、地点、商家、商城分类和商品关联内容拆成独立失败边界；次要接口失败不再遮蔽主内容，并补齐加载更多、评论、有用和删除失败反馈。
- 全站受保护路由和登录操作统一携带经过站内校验的返回地址，直接刷新、登录过期重登及商品/帖子/甄客验互动后均能回到原任务页面，同时阻止外部开放重定向。
- 清理 `commerce.less` 中旧商城绿色作为页面结构色的残留，支付品牌色和成功语义色继续保留；登录页错误品牌字形修正为“甄”。
- 第三轮验收将每个甄客帖/轮播管理动作同时绑定超管角色和独立权限，避免方法级权限覆盖类级角色保护；增加反射测试锁定全部管理端点。公共帖子查询同时收口失效地点和未审核/停用/删除商家的可见性。
- 发帖媒体上传增加并发预占，防止快速多选突破 9 个媒体或 1 个视频上限；后端新增伪装 JPG、不可解析 MP4 和不支持 GIF 的拒绝测试。帖子详情复用正式微信 JS-SDK 分享卡片，普通浏览器继续使用系统分享或复制链接。
- `/profile/posts` 补齐分页加载和不可点击的已删除状态；商城 `scene`/`keyword` 与 URL 双向同步，浏览器前进后退及清空搜索不会丢失场景；需预约的本地生活商品必须填写预约说明。
- 第四轮验收修复浏览器 WGS84 设备坐标交给腾讯逆地址解析时缺少 `coord_type=1` 的坐标偏移风险，并增加 URI 契约测试；首页优先恢复会话内已有的设备/手动定位，不再每次返回首页都重新申请定位并覆盖用户选择。
- 三个稳定本地生活分类现在由后端禁止删除、管理端明确禁用删除入口；历史商品重新上架时，后端会重新校验套餐内容、使用须知、有效期、退款/过期规则及预约说明，不能绕过新建/编辑时的完整性约束。
- 按用户明确要求，用户端和管理端 `.umirc.ts` 保持仓库原有默认：`/api` 指向 `https://dzshop.vip`，本地 `8080` 配置继续注释，由用户测试时自行切换；两个文件没有内容差异，不进入本轮提交。
- 第五轮按用户最新口径纠正交易信息架构：四个并列大类是商城、酒店、景区、饭店；商城不是被三个新增分类替代的“全部”，而是原商城业务的完整聚合。后端新增只读分页筛选契约，让商城大类排除三个本地生活稳定代码而不新增或改写任何旧分类；三个新增大类继续复用现有商品、订单、支付和核销底座。
- 第六轮继续对照 `origin/main` 找回原商城业务：恢复正在招募试用与推荐甄客验的发现流，并让它跟随四大营业分类、原动态分类和关键词筛选；甄客验上传改为用户隔离目录，发布时验证当前账号归属与真实文件存在，报告/媒体写入结果全部纳入事务失败条件。
- 第七轮复核“本地前端连接远端后端”的真实联调方式：保持用户指定的两份 `.umirc.ts` 原样，不依赖 `/profile` 开发代理掩盖问题；甄客帖上传、列表、详情、评论头像及轮播 API 现在把数据库中的平台相对媒体路径转换为当前后端可访问的完整 URL，发布时再安全还原为当前账号已登记的相对资源路径。后端 77 项测试通过，原商城路由与服务导出逐项对照 `origin/main` 未发现删除，三个本地生活稳定分类仍由服务端强制走 `OFFLINE`，其余原商城分类继续保留线上配送、已有线下核销或两者兼有的真实商品配置。
- 第八轮按用户再次强调的“只扩展、不改写原商城业务”逐字复核用户端文案：修正首页“去商城”和交易保障区可能把整个商城误读为仅套餐核销的表述，明确原商城商品配送与本地生活套餐核销并存；四模块入口顺序与查询仍为商城、酒店、景区、饭店，数据库只新增后三个稳定分类。
- 第九轮按用户最终确认的业务定义固化 Cloud 约束与仓库入口说明：商城完整保留自营与商家商品、动态分类、试用、线上购买与发货物流、原有线下核销、退款和甄客验；酒店、景区、饭店只是同一交易底座上新增的三个稳定分类。同步替换过时 README，清除旧品牌、Mock、错误 Java 版本和示例账号指引；修正迁移菜单 SQL 对未使用 `query` 列的依赖，以及支付兜底、赛事 SSO 和 AI 提示中的旧平台称呼。SQL 仍未执行，两份 `.umirc.ts` 保持原样。
- 第十轮把“只扩展、不改写”落实为订单层回归用例：普通商城商品仅支持配送时继续配送、仅支持原有线下核销时继续核销、同时支持两者时尊重用户选择；三个新增稳定分类仍强制 `OFFLINE`。首页和商城入口文案同步明确原商城存在配送与核销两类履约，避免把旧业务误写成只有线上物流。
- 第十一轮继续按“四类并列、只扩展不收缩”审计管理端：订单状态现在结合真实履约方式解释，原商城配送订单继续显示“待发货 / 待收货 / 已完成”，原有线下商品及酒店、景区、饭店订单显示“待使用 / 已核销”；线下订单详情展示核销信息且不再伪造收货地址，筛选文案同时涵盖两种履约。甄客帖新评论返回补齐昵称、头像、回复对象和帖主标识，与评论列表保持同一数据契约并增加稳定排序。
- 第十七轮按用户验收反馈取消首页轮播外链域名白名单及环境变量，仅保留完整 HTTPS、有效主机名和禁止用户凭据的校验；腾讯地图 URI API 仍保留必需的登记应用名 `referer`。用户端首页、商城、发布、商家、套餐、甄客验及订单流程移除面向用户暴露的“原商城、交易底座、动态分类、服务端、真实数据、逻辑删除”等内部实现说明，改为直接的消费与发布文案。商家选择接口在后端和前端双重过滤空项，避免异常响应数组中的 `null` 触发发布页崩溃；用户本地修改的两份 `.umirc.ts` 保持原样且不纳入本轮提交。
- 第十八轮修复首页轮播有效期保存时的日期反序列化错误：管理端取消时分秒选择，只提交 `YYYY-MM-DD` 自然日；后端使用明确的日期类型接收，并按服务器时区把开始日期保存为 `00:00:00`、结束日期保存为 `23:59:59`，同一天起止也合法且不会发生 JSON 默认时区造成的跨日偏移。扫描确认优惠券、试用截止日期和甄客帖筛选本就只提供日期选择；订单、支付、审核、日志等系统产生的精确时间不属于用户日期选择，保持原有时刻数据。
- 第十二轮继续清理“加载失败被误报为空数据”的验收缺口：统一“我的”资产列表、优惠券/订单/试用详情、甄客验详情及评论、地点/商家/甄客帖详情都提供可恢复失败态；首页轮播与甄客帖管理也区分加载失败和真实空列表。修复商城商品第二页以后关闭总数统计导致“加载更多”提前结束的问题，并用分页契约测试锁定后续页面仍返回真实 `total`。同时增加商品保存层回归测试，证明普通商城分类继续保留线上配送与原有线下核销可同时启用的配置，只有酒店、景区、饭店三个新增稳定分类会被强制改为 `OFFLINE`。
- 第十三轮从真实购物车与支付路径继续复核“只扩展、不破坏原商城”：购物车接口现在返回商品真实的线上/线下履约能力，结算页不再把原商城普通线下核销商品误判为配送商品或强制要求收货地址；同一商家的配送与核销混合购物车按真实拆单规则禁用优惠券，避免前端先给出优惠再被服务端拒绝。混合履约旅程、地址/购物车加载失败、商品/订单/优惠券结算失败均有明确状态与重试。订单支付页和支付成功页每次都从服务端重新确认最新订单，不能使用支付前缓存冒充结果；JSAPI、H5 回跳和 Native 扫码支付的成功提示按配送/核销区分，扫码轮询增加防重入和可见失败态。
- 第十四轮继续复核配送与核销混合购物车的后端拆单结果：收货地址快照现在只写入 `ONLINE` 配送订单，不再把用户地址附着到同一购物车拆出的 `OFFLINE` 核销订单，并增加履约类型回归测试锁定该最小化数据边界。
- 第十五轮按全路由矩阵继续审计原商城直达链路：甄客验详情现在归入商城交易内容导航，不再错误高亮首页；订单列表、订单详情、试用列表和试用详情的物流弹窗统一清空上一笔轨迹，查询失败时展示当前业务对应的明确失败态与原地重试，并用请求序号丢弃关闭弹窗或切换对象后的迟到响应，避免旧物流数据被误认成当前订单结果。
- 第十六轮继续收紧订单与试用核销凭证：切换业务对象时立即清空上一笔二维码，请求结果必须与当前订单或试用申请 ID 一致才可展示，定时轮询增加防重入，避免慢响应或重叠请求把旧核销码覆盖到新弹窗。
- 第十九轮最终口径把“甄必享”改为与甄客帖、商品均独立的平台官方内容：甄客帖恢复原有用户发布闭环且完全移除甄必享分类；超管在独立管理页创建、编辑、排序、发布、下线和删除甄必购、甄必吃、甄必玩、甄必住，用户端首页、列表和详情读取真实官方内容，普通用户仅能浏览、点赞、评论、回复和删除自己的评论。商家角色不显示菜单且接口以 admin 角色和独立权限双重保护。商家新建商品不再预填售价，库存默认无限且可切换有限库存；有限库存沿用原子扣减/回补，无限库存仍经过同一库存 Mapper 入口但数量保持不变，存量商品迁移后继续保持有限库存语义。用户确认 V1 已在本地执行后，V1 保持仓库原始内容且不再改写，本轮官方内容表、权限和库存字段全部迁入独立 V2；SQL 均未由 Codex 执行。
- 用户端与管理端 typecheck、build 均通过；管理端构建仍有退出阶段缓存持久化权限警告但退出码为 0。`ruoyi-shop -am test` 最新复跑为 91 项全通过，0 failures / 0 errors / 0 skipped，其中新增 5 项甄必享服务测试及超管动作权限回归；`git diff --check` 通过。沙箱内 Maven/esbuild 因工作区目录读取策略失败后，使用获批的沙箱外同命令复跑成功，未把环境失败冒充源码验证。浏览器以 `390x844` 和 `1440x900` 检查甄必享列表的分类横滑、空态和桌面四列布局，未发现横向页面溢出；本地库未执行 V2，因此未把真实官方内容 CRUD/互动声称为运行时已验收。

2026-08-28 PR 本地二次审查与优化：

- 对比 `main...codex/cloud-zhenkexing-v1` 的甄客行扩展并复核 Controller、Service、Mapper/XML、V2 迁移、用户端全路由及三项超管页面；没有改写 V1、没有执行 SQL、没有部署或连接生产库。
- 修复甄必享并发点赞的假失败：唯一关系已被并发请求写入时不再因 `INSERT IGNORE` 返回 0 向用户报错，而是重新读取最终持久化状态。
- 收紧帖子和甄必享评论写入：评论 SQL 只从仍公开、未删除的父内容条件插入，避免详情校验之后恰逢管理员删除/下线仍留下新评论；新增 4 项服务测试覆盖点赞及两类评论竞态。
- Ant Design 静态消息持有者接入统一主题，登录验证码表单只在真实挂载后重置；浏览器新页面复核不再出现此前的静态主题和未连接表单告警。
- 首页自动定位失败改为不遮挡导航的页内可操作提示，可直接打开手动选城；轮播使用单一页面级标题和二级内容标题。商户商品卡改为正式链接，补齐键盘访问与可见焦点；登录页技术实现文案改为用户收益表达。
- 本地已运行服务返回 `/health` 和 `/api/health` HTTP 200。真实 API 返回 1 条甄客帖、2 条轮播和 6 个商品；V2 内容表未执行，因此甄必享为真实空列表，不伪造数据。
- 浏览器在 `390x844` 与 `1440x900` 复核 `/`、`/posts`、帖子详情/发布登录回跳、地点详情、甄必享列表/删除态详情、商城、商品、商户、结算登录回跳、统一我的、订单和甄客验详情；未发现页面横向溢出。管理端仅完成登录页双视口与静态代码/权限契约复核，因没有现成登录态或凭据，没有声称超管 CRUD 已完成本地运行时验收。
- 最新验证：两个前端 `typecheck` 通过；两个前端生产构建通过（管理端仍有退出阶段缓存持久化权限警告但退出码为 0）；`ruoyi-shop -am test` 共 95 项通过，0 failures / 0 errors / 0 skipped；`git diff --check` 通过。

2026-08-28 甄必享地点专题产品化：

- 按用户最新要求和 `yuanxing/` 地点专题视觉，将甄必享从“官方账号发布的帖子卡”重构为独立的官方地点专题：标题/地点先于内容，大图与多图画廊为视觉主体，首屏展示服务摘要、地址、营业/开放时间、电话、地图导航和“发布甄客帖”，随后才是官方详细攻略与点赞评论。
- 超管编辑器取消地点名称和地址自由填写，改为腾讯地图 POI 搜索选择；请求仍携带完整选点快照以便校验，但服务端必须根据 `provider + providerPlaceId` 再次查询地图详情并保存统一 `shop_place`，最终名称、地址、坐标不信任前端提交值。
- 专题图片改为 1 至 9 张有序资源，第一张为列表封面；管理端支持上传、预览、前后调整和删除。新增首屏服务摘要、开放时间、公开电话和 20000 字详细攻略，发布前校验地点、摘要、封面和资源完整性。
- 新增幂等迁移 `delivery-backend/migrations/20260828_zhenkexing_v3.sql`，只追加统一地点关联、服务字段和专题图片表；按用户确认的生产事实，甄客行尚未投产且不存在旧甄必享数据，因此不做旧封面回填、旧专题下线或兼容迁移，真实地点和首屏服务摘要直接设为数据库必填。V1/V2 未改写，本轮没有执行 SQL。
- 专题详情的“发布甄客帖”携带 `placeId`，发布页从公开地点接口加载并预选该真实地点；用户仍独立发布甄客帖，不形成投稿、归类或官方背书。
- 两个前端本轮 `typecheck` 与生产构建均通过；管理端构建仍只有退出阶段缓存持久化权限警告且退出码为 0。后端先以相关测试确认 32 项通过，再在沙箱外完整复跑 `ruoyi-shop -am test` 共 96 项通过，0 failures / 0 errors / 0 skipped。`git diff --check` 通过。
- 本地浏览器在 `390x844` 与 `1440x900` 检查 `/enjoy` 的分类横滑、真实空态和页面宽度，`scrollWidth <= clientWidth` 且控制台无 warning/error；管理端 `/admin/zhenke-enjoys` 正确进入后台登录保护。因 V3 未执行且没有管理员登录态，本轮没有伪造专题数据，也没有声称详情画廊和超管编辑器已完成真实运行时验收。
- 用户首次在已有 V2 本地调试数据上执行 V3 时，MySQL 已为新 `NOT NULL place_id` 产生无效占位值，添加 `fk_ze_place` 因找不到父地点报 1452；DDL 已部分提交。用户确认本地甄必享测试数据可以删除并已自行清空评论、点赞和主表，因此 V3 恢复为生产空表首次安装的严格版本：真实地点和首屏摘要直接 `NOT NULL`，并补齐 `fk_ze_place`；不再保留临时兼容分支。Codex 没有代为连接数据库或执行删除。

2026-08-28 管理端菜单路由高亮修复：

- 修复甄客帖管理、甄必享管理和首页轮播管理进入正确页面后，侧栏选中项与顶部标题却回退到数据看板的问题。根因是旧逻辑直接把 URL 首段当作内部菜单 key，而这三项分别使用 `zhenke-posts`、`zhenke-enjoys`、`home-banners` 路径，与 `zhenkePosts`、`enjoys`、`banners` 菜单 key 不同。
- `getAdminNavKey` 改为根据统一的 `adminNavPaths` 反向匹配，兼容 `/admin` 基路径、尾斜杠、查询参数和详情子路径，避免后续再出现路径名与菜单 key 耦合。
- 管理端 `typecheck`、生产构建和 `git diff --check` 通过；在用户现有 Edge 超管登录会话中逐项验证三个菜单的点击进入与整页刷新，URL、侧栏选中项、顶部标题和页面标题均一致，订单管理回归正常，控制台无 warning/error。用户本地 `.umirc.ts` 未修改。

2026-08-28 全站媒体上传与本地运行优化：

- 按用户最新要求移除甄必享编辑器中“地址来自地图选点”的实现说明；真实地点仍必须通过地图 POI 搜索选择，服务端仍按地点 ID 二次确认，未放开地点名称、地址或坐标手填。
- 甄客行相关图片、视频和证明材料入口统一改为点击选择或拖拽上传，覆盖甄必享、首页轮播、商品封面/主图/详情图、商品认证材料、甄客帖、甄客验、商家营业执照/门店证明、头像及扫码图片导入；不再提供媒体 URL 手填框，也不再要求媒体使用 HTTPS。首页轮播的第三方跳转目标仍按既定安全边界只允许完整 HTTPS 外链，该规则与媒体上传无关。
- 新写入媒体统一保存为环境无关的根相对路径 `/profile/upload/...` 或 `/profile/avatar/...`；前端展示时按当前环境映射为 `/api/profile/...`，因此本地走本地代理、生产走生产域名。后端可兼容上传接口历史返回的本地/生产 HTTP(S) 预览地址，但入库前一律还原为相对路径。
- 新增统一后端媒体路径校验：拒绝外部非平台文件、凭据、查询参数、片段、控制字符、反斜杠、编码穿越及目录穿越；保存前校验文件确实位于配置的 profile 目录，图片还会解码确认真实图片内容。商品新媒体同时校验当前商家目录归属，原商城已有商品的旧媒体值仅允许在未改动时原样保留。
- 用户端和管理端 `typecheck`、生产构建全部通过；管理端构建仍只有退出阶段缓存持久化权限警告，退出码为 0。后端在正常本机权限下完整复跑 `ruoyi-shop -am test` 共 101 项通过，0 failures / 0 errors / 0 skipped；受限环境中出现的 JUnit 临时目录 `AccessDeniedException` 已明确归类为沙箱权限并由同命令正常权限复跑排除。
- 本地重新打包并真实启动 Spring Boot，Tomcat 监听 `8080`，用户端 `8000`、管理端 `8001/admin` 均启动成功，两个前端通过 `/api/health` 访问后端返回 HTTP 200。真实上传接口返回 `/profile/upload/...` 相对路径，本地 HTTP 读取图片返回 200；该临时测试文件已按精确路径删除，没有保存轮播或其他业务测试数据。
- 使用用户现有 Edge 登录态检查：甄必享新增表单无旧提示且只显示地图搜索与图片拖拽上传；首页轮播只显示图片拖拽上传；用户发帖、头像和管理端商品封面/主图/详情图均显示点击或拖拽入口，旧媒体通过 `/api/profile/...` 正常预览。Edge 扩展自身禁止自动注入本地文件，因此未绕过浏览器安全策略；页面控件、真实上传接口和相对路径读取已分别完成验证。
- 用户本地修改的两份 `.umirc.ts` 保持不变；本轮没有执行 SQL、部署、提交或推送。

2026-08-28 API 时间契约与请求失败展示收口：

- 后端 Jackson 全局日期时间输出统一为 `yyyy-MM-dd HH:mm:ss`，保留字段级 `@JsonFormat` 对纯日期字段的覆盖；新增测试锁定无注解 `Date` 的精确输出及字段级覆盖优先级。甄客帖、甄必享等真实 API 已确认不再返回 ISO `T`、毫秒或时区后缀。
- 管理端和用户端凡展示完整日期时间的地方均直接使用后端字符串，不再用 `toLocaleString`、`dayjs` 或截取替换进行二次格式化；日期筛选、表单提交和时间比较逻辑保持不变。
- 甄客帖管理、甄必享管理、首页轮播管理移除请求失败时额外铺设的大块红色错误面板，加载失败沿用统一消息提示；用户端同类“toast 之外再显示一整块刷新错误”的重复组件与调用一并移除。审核驳回、库存不足、支付未确认、登录权限、主体不存在等真实业务/安全状态继续保留。
- 最新验证：两个前端 `typecheck` 和生产构建通过；管理端构建仍只有退出阶段缓存持久化权限警告且退出码为 0；后端 `ruoyi-shop -am test` 共 102 项通过，0 failures / 0 errors；`git diff --check` 通过。用户现有 Edge 超管登录会话及原始 API 响应均已验证甄客帖、甄必享发布时间显示为 `YYYY-MM-DD HH:mm:ss`。

## G. 最终审计与 Git 交付

- [x] 对照 `CODEX_CLOUD_GOAL.md` 完成定义逐项复核。
- [x] 确认无 Mock、假上传/定位/发布、硬编码分类 ID 或正式代码对 `yuanxing/` 的运行时依赖。
- [x] 确认旧绿色商城品牌未在用户端形成独立视觉体系。
- [x] 确认 SQL 未执行，仓库无密钥、数据库快照、日志、`node_modules`、`dist`、`target` 或浏览器产物。
- [x] 已创建 GitHub PR #7；本地已拉取 PR 源提交并在独立分支持续修复，未自动合并或部署。
- [x] 最终报告区分代码验证、运行时验证、未执行 SQL、缺少密钥的联调和用户真实环境验收。
- [x] 最终复跑用户端与管理端 typecheck/build、后端 82 项测试及 `git diff --check`；两份 `.umirc.ts` 仍保持用户要求的原代理配置。

提交与 PR：

Cloud 原任务内部阶段提交记录：`8e547f2` 后端与迁移、`633b67f` 用户端、`645161e` 超管端、`7bf5b5a` 用户端结构重写、`5e6aa52` 超管工作流、`b50bb13` 旧绿色品牌清理、`07af429` 地点/媒体/治理安全、`e2c9899` 本地生活交易体验、`c59cf15` provider 地点校验、`6293ad5` 运行时路由与视觉回归、`ca9cfd5` 履约与归属测试、`c825c0a` 线下支付成功态。平台最终压缩为 GitHub 提交 `e65f715`，PR 为 `https://github.com/shuaibo888/zhenke/pull/7`。2026-08-27 的本地代码验收修复也已按领域提交到同一 PR 的 `origin/codex` 分支，以 PR 最新提交为准。

## 交付后用户真实环境验收

- 浏览器连接本地开发服务曾被桌面端安全策略阻止，没有绕过，也不把构建结果冒充运行时结果。用户已明确从本次提交后接手测试调试：先人工执行增量 SQL、配置真实地图与微信环境，再完成关键页面双视口、支付回调、配送、核销、甄客验资格和生产数据验收。

## 2026-08-31 PR 产品审查与体验收口

- 首页甄必享参考 `yuanxing/` 的内容编排方向，从四个平行入口改为“甄必购 / 甄必吃 / 甄必玩 / 甄必住”四段纵向专题；每段独立呈现标题、说明、内容状态、查看全部和横向内容卡，桌面与手机端均保持清楚的阅读顺序。四类请求相互隔离，单类失败不再拖垮整块首页；加载中与真实空态分开表达。
- 首页定位增加请求版本保护：用户手动选择城市后，迟到的设备定位结果不会再覆盖其选择。轮播图片按首屏优先级延迟解码与加载；2026-08-31 按最新要求保持自动轮播并移除手动暂停控件。地点、甄客帖、甄必享、商城商品流均增加迟到响应保护，避免快速切换筛选后旧数据覆盖新页面。
- 甄客帖身份状态写入 URL，返回与分享时保留本地土著/外地游客/在外家乡人视角；地点详情补齐真实分页和带地点上下文的发布入口；发布页商家搜索去抖且媒体按用户选择顺序稳定入列。甄必享与甄客帖详情将正文、评论和回复拆为有上限的分页请求，回复只首屏预览，避免一次加载完整评论树。
- 超管轮播和甄必享新增/编辑请求不再接受客户端状态字段：新内容统一保存为停用，编辑保留持久化状态，只有独立启停权限接口可以发布或下线。公共接口改为最小字段视图；甄客帖作者不能给自己的帖子标记“有用”。
- 有限库存订单项新增实际扣库存快照，并在取消、超时或直接退款时按订单快照决定是否回补，避免商品后来切换无限库存模式造成错误回补。增量脚本为 `delivery-backend/migrations/20260831_zhenkexing_v4.sql`，未执行；V1/V2/V3 保持不变。若 V2 到 V4 之间曾切换过库存模式，执行前需先核对对应未完结订单。
- 资源保护仅保留代码层轻量边界：图片读取元信息后校验像素与边长，不再为校验完整解码；MP4 结构校验改为流式读取并限制嵌套深度。按用户最新要求，所有 OOM 相关 YML 改动均已撤回，本轮 `.yml/.yaml` 差异为零；两份本地 `.umirc.ts` 仍为用户自己的代理改动，没有纳入本轮编辑。
- 验证证据：用户端与管理端 typecheck 通过，两个前端生产构建通过；相关后端 58 项测试通过，0 failures / 0 errors；首页在 `1440x1000` 与 `390x844` 实际检查纵向甄必享分组，浏览器控制台 0 error。视觉检查连接的是当前前端与本地既有后端，仅证明页面渲染和响应式表现，不代表 V4 已执行或新版后端、真实支付、微信、地图和生产业务闭环已完成运行时验收。

## 2026-08-31 `yuanxing` 首页帖子与三身份样式对齐

- 首页甄客帖从多行响应式网格改为单行横向故事轨道，桌面端保持横向排列，手机端显示一张主卡并露出下一张作为滑动提示；不再出现第三张掉到下一行的瀑布式效果。
- 完整帖子流移除“推荐 / 本地专区 / 外地专区”，只保留“本地土著 / 外地游客 / 在外家乡人”三种身份切换。前端使用 `perspective` URL 状态，后端分别按 `LOCAL`、`TOURIST`、`HOMETOWNER` 视角字段分页查询；城市内容开关默认关闭，开启后再在身份条件之外叠加当前市条件。
- 帖子卡片与详情统一展示“地点城市 · 身份”，城市读取发布时保存的地点快照，具体地点名称仍单独展示。发布页改为先选真实地点，再明确选择与该城市的关系；取消默认“本地土著”，避免未操作即产生错误身份数据。
- 首页轮播保持自动播放，移除暂停/播放按钮；轮播点居中，压缩桌面图片与文案区高度，让首屏更早进入甄客帖内容。手机端“查看全部”保持单行，不再被挤成两行。
- 验证证据：用户端 typecheck 与生产构建通过；干净重新编译后 `ShopZhenkeServiceTest` 27 项通过，0 failures / 0 errors；浏览器在 `1440x1000` 检查首页横向轨道，在 `390x844` 检查首页露边滑动、三身份页面和城市标签，身份切换 URL 为 `?perspective=TOURIST` 且返回对应帖子，控制台 0 error。未执行 SQL、未修改 YML、未部署或提交。

## 2026-08-31 甄客帖与甄必享城市内容运营开关

- 新增参数键 `shop.zhenkexing.cityFilterEnabled`：缺失或 `false` 保持当前全平台内容，`true` 时甄客帖和甄必享公共列表同时严格按用户当前定位/手选城市过滤；三种帖子身份继续作为城市范围内的独立条件。
- 后端直接读取 `sys_config` 参数行，适配运营直接更新数据库的方式，不依赖参数 Redis 缓存刷新；开关开启但请求缺少城市时明确拒绝，绝不静默回退全平台内容。帖子详情、甄必享详情和指定地点帖子列表不受公共内容流开关影响。
- 用户端不增加配置管理页面或开关按钮，只把浏览器会话中已保存的市级名称无感附加到甄客帖/甄必享列表请求；首页定位或手选城市成功后重新加载两类内容。
- 新增未执行的 `delivery-backend/migrations/20260831_zhenkexing_v5.sql`，幂等写入默认 `false` 参数并补充帖子城市、地点城市查询索引。未修改任何 YML。
- 验证证据：用户端 typecheck 与生产构建通过；城市开关、两类内容服务及 Mapper 城市条件共 42 项针对性测试通过，0 failures / 0 errors。SQL 未执行，开关开启后的真实数据库列表结果仍需部署环境验收。

## 2026-08-31 首页/甄客帖加载与发帖商家选择优化

- 首页原实现首屏并发调用帖子、轮播和四个甄必享分类共 6 个接口，设备定位成功后会再次请求整组内容；帖子列表还逐帖查询媒体：首页 9 条最多形成 9 次附加媒体 SQL，完整帖子页 12 条最多形成 12 次附加媒体 SQL。优化后首页改为一个 `/shop/zhenke/home` 聚合请求，后端只解析一次城市开关，并保留帖子、轮播、甄必享三块独立失败信息。
- 甄必享首页四分类改为 MySQL 8 窗口函数一次取出每类前 2 条；帖子列表媒体改为按本页 `post_id` 一次批量查询。按当前实现计，首页由 6 次 HTTP、约 25 次数据库访问降为 1 次 HTTP、约 5 次数据库访问；完整甄客帖首屏由约 15 次数据库访问降为约 4 次，不删内容、不改变分页、城市或身份规则。
- 发帖页商家选项为空的根因已确认：项目 MyBatis 未开启下划线转驼峰，新 SQL 使用 `resultType` 自动映射 `merchant_id/shop_name`，返回对象字段为空后又被服务层过滤。已改为显式 `resultMap`，并用 `company_name` 兼容缺少门店名的历史商家；审核通过、启用且未删除的安全条件保持不变。现有选择框增加中文加载与空态，没有新增配置页。
- 验证证据：用户端 typecheck 与生产构建通过；后端 `ruoyi-shop -am test` 共 126 项通过，0 failures / 0 errors，覆盖首页聚合、批量媒体、甄必享分组、城市范围与商家字段映射。沙箱内首次构建和全量测试因 Windows 目录权限失败，改在正常权限环境复跑后全部通过；新版后端运行时仍待部署环境验收。未修改 YML、未执行 SQL、未部署。

## 2026-08-31 用户端 `yuanxing` 强视觉对齐重做

- 本轮只调整 `delivery-frontend` 与进度记录，不修改管理端、后端、数据库迁移或任何 YML；真实路由、API、登录、交易、发布、评论和资产能力保持原契约。
- 全局设计系统重新收口为原型的米白画布、白色内容卡、暖橙主操作、深棕标题、16–20px 圆角与轻阴影；桌面沿用正式响应式布局，手机端重点贴近原型的顶部品牌、悬浮发布入口和四项底部导航。
- 首页轮播改为摄影封面叠字样式，关闭悬停暂停与图片放大遮罩；甄客帖首页保持横向故事轨道，完整帖子页在窄屏改为单列摄影卡；删除城市运营开关与身份判定的用户侧技术提示。
- 甄必享保持甄必购、甄必吃、甄必玩、甄必住四组纵向编排，组内内容改为图左文右；地点与商家信息首屏从深色渐变改为轻量白卡。商城四模块、商品卡、登录、结算、订单、优惠券、积分、试用和报告页通过共享样式统一到同一原型语言。
- 个人中心保留产品要求的内容/履约/权益/参与/安全分组，但首屏恢复原型式橙色身份卡，并压缩分组卡片与入口密度，避免继续呈现后台仪表盘感。
- 按最新反馈继续做减法：首页甄必享删除购/吃/玩/住空态中的装饰图标、虚线大框、英文序号和分组差异渐变，改为与 `yuanxing` 相近的白色专题卡、短标题、短说明和文字分隔；真实内容仍使用图左文右的摄影卡，查看全部与失败重试保留。
- 通用空态只在加载时保留旋转反馈，不再为“暂无内容/加载失败”铺设收件箱或断线插画；商城承诺说明及四大营业模块删除重复表达的图标/单字方块，地点、商城、甄必享和个人中心首屏删除大号背景水印与装饰光斑。定位、搜索、导航、购物车、发布、交易状态和认证等有实际含义的图标继续保留。
- 商城首屏在保持四类业务并列关系的前提下补足真实选购信息：搜索前置到首屏，四个模块明确区分好物、住宿、游玩和餐饮的履约方式，并以三条简短文字说明集中交代商家信息、配送/预约/核销及有效期规则；不增加装饰性入口或虚构服务。
- “我的”全部正式子路由统一使用“我的甄客帖”版式：页面左右留白、顶部返回信息卡、下方内容卡、标题/数量分栏和暖色空态保持一致，覆盖甄客验、订单、优惠券、积分、积分明细、试用及订单/优惠券/试用详情。移除重复的大标题、优惠券水印和无业务作用的空态图标，原有筛选、详情、支付、退款、物流、核销、兑换和发布甄客验能力不变。
- 按用户此前明确要求，本轮不启动浏览器自动化；视觉运行时仍需用户在本地真机/浏览器确认。代码验证结果见本节后续补充。
- 验证证据：用户端 `typecheck` 通过；用户端生产构建在受限沙箱内因 esbuild 目录读取权限失败，使用正常权限以同一 `npm run build` 命令复跑成功；`git diff --check` 通过。未运行浏览器、未执行 SQL、未部署、未提交或推送。

## 2026-08-31 白底视觉收敛、帖子城市双轴与两步发布

- 按老板最新审美口径将用户端主画布从米白改为纯白，取消“甄客行”前的橙色字标和当前城市前的定位图标；当前城市常驻标题右侧，首页内可直接打开选择器，其他页面点击后回到首页并自动打开同一选择器。桌面/手机导航、卡片、商城、个人中心、帖子和甄必享统一减少大圆角、渐变、悬浮位移与重阴影，标题统一为常规无衬线字体，保留摄影内容与暖橙主操作色。
- 首页轮播删除暗色蒙层，图片保持明亮；文案改为浅色实底信息区，自动轮播规则不变。首页甄必享继续按购、吃、玩、住纵向编排，分组改为白底线性分隔，不再依赖层层嵌套的圆角容器。
- 甄客帖列表改为两个独立筛选轴：顶部固定“本地土著 / 外地游客 / 在外家乡人”，左侧从已发布帖子实时查询城市列表；点击城市后以 `postCity` 单独过滤帖子。新增只读 `/shop/zhenke/posts/cities` 接口和 Mapper 分组查询，不新增表、不改迁移、不改 YML。城市运营配置缺失或为 `false` 时，城市候选覆盖全平台且首页甄客帖/甄必享仍为全平台内容；配置为 `true` 时，定位城市先约束公共范围，左侧只能出现该范围内真实发布过帖子的城市。
- 发布甄客帖改为两步：第一步只选择并预览图片/视频，至少有一张图片后进入第二步；第二步顶部展示媒体并允许追加，正文、地点、身份、商家和建议使用线性表单。删除用户点名的四条说明、当前位置优先开关、英文装饰标题及字段图标；真实地点二次校验、商家安全条件、媒体数量/类型/大小/时长和上传归属校验保持不变。
- 验证证据：用户端 `typecheck` 与生产构建通过，`git diff --check` 通过；后端在正常权限环境干净编译并执行 `ShopZhenkeServiceTest`、`ShopZhenkeCityScopeServiceTest`、`ShopZhenkeHomeServiceTest` 共 34 项，通过 34、失败 0、错误 0。Edge 使用本地 `http://localhost:8000` 前端连接生产 `https://dzshop.vip` 后端检查：首页、城市选择弹窗、甄必享和商城均为白底且控制台 0 error；轮播图片明亮、标题右侧城市无定位图标。联调时标题显示北京而首页仍出现定州甄必享，符合生产配置为 `false` 时不按定位过滤的规则。生产后端尚未包含本轮 `/posts/cities` 接口，因此城市双栏真实数据需部署新版后端后复核；本轮未发布帖子、未写生产数据、未执行 SQL、未部署、未提交或推送。
- 2026-08-31 经用户明确确认，重新构建用户端、管理端和生产后端 JAR 后使用公司固定无 `down` 发布流程部署，发布 ID 为 `20260831-102404-26356`，备份保留于 `/root/zhenke/.deploy-backups/20260831-102404-26356`。Compose 最终状态为 Redis 健康且未重建、后端持续运行、UI 健康；启动阶段前 5 次健康检查为短暂 502，第 6 次通过 HTTP 200，随后公网 `/api/health` 返回 `UP`，新增 `/api/shop/zhenke/posts/cities` 返回业务码 200。发布未修改 YML、Compose、数据库或服务器基础设施，也未执行 SQL。

## 2026-09-01 手机微信内容卡片分享

- 按最新范围只保证从当前实现开始的内容，不增加历史参数链接兼容、历史数据补偿或数据库迁移。甄客帖、商品、公开试用、甄客验和甄必享详情统一接入微信公众号 JS-SDK；页面加载后主动注册好友与朋友圈卡片，用户点击分享时再次注册，两个现代分享接口成功后才展示右上角原生菜单引导，并保留旧微信客户端接口兜底。
- iOS 使用首次进入 WebView 的无 `#` URL 验签，Android 使用当前详情 URL；配置调用串行化并允许同 URL 重试。试用详情直接校验并加载路由中的公开活动，不再依赖商品页前四条试用列表猜测活动。
- 分享 `link` 统一改为同源公开卡片端点。后端按内容实时读取标题、摘要与首图，返回可抓取的 `og:*`/itemprop 元数据页，打开卡片后转到正式详情；图片端点只读取已校验的平台图片，中心裁剪为 360×360 JPEG，缺图或图片异常时返回统一品牌兜底图。试用活动会校验公开状态与商品归属；甄客验标题补齐商品品牌；签名响应禁止缓存，公开域名固定从 `wechat-pay.frontend-return-url` 派生，不信任请求 Host。
- 验证证据：用户端 `typecheck` 与生产构建通过；后端在正常权限环境完成干净编译，微信签名、四类内容元数据、试用归属/降级、方形图片及缺图兜底共 9 项专项测试通过，0 failures / 0 errors；`git diff --check` 通过。未部署、未做真实公众号接口调用或 iOS/Android 微信真机发送验收，公众号后台 JS 接口安全域名与服务器出口 IP 白名单仍须在部署环境确认。
- 2026-09-01 经用户明确确认，重新执行生产后端完整打包（135 项测试通过）及两个前端生产构建后，使用公司固定无 `down` 发布流程部署，发布 ID 为 `20260901-075947-57628`，备份位于 `/root/zhenke/.deploy-backups/20260901-075947-57628`。Redis 保持原容器健康运行，后端与 UI 重建后持续运行且 UI 健康；启动阶段前 5 次公网检查为预期的临时 502，第 6 次 `https://dzshop.vip/api/health` 返回 HTTP 200，JSON 状态为 `UP`。公众号签名接口返回业务码 200 且包含签名；甄客帖示例的 HTML、JPEG 均为 200，商品、公开试用、甄客验、甄必享在线卡片端点也均返回 200 且包含标题和图片元数据。备份清理后只保留当前发布与上一发布两份；本次未修改或发布 `.env`、Dockerfile、Compose、Nginx、数据库、证书及服务器基础设施。
- 首次生产真机验收发现 iPhone 微信 8.0.75 通过原生“发送给朋友”仍生成纯链接。生产日志与用户 16:04 的操作逐秒对应：JS-SDK 签名返回 200，三秒后微信原生网络栈成功读取 360×360 JPEG；同分钟无卡片页/图片 4xx、5xx、WAF 或网关错误，排除复制粘贴、签名失败、封面不可达和服务端抓取故障。前端此前把内部 `/api/shop/wechat/share/...` 作为实际 `link`，并在现代客户端同时注册现代与废弃菜单接口，存在被客户端降级或旧处理器覆盖的风险。
- 待二次发布修复已将实际分享目标改回公开 canonical 详情地址，保留正整数试用活动参数，并增加 `zkShare=20260901-2` 以避开微信旧失败缓存；封面继续使用安全图片端点。客户端先用 `checkJsApi` 确认真正支持 `update*`，现代客户端不再注册废弃 `onMenuShare*`，仅旧客户端缺少现代能力时兜底。另修复 jweixin 1.6.0 在同一 SPA document 二次 `wx.config` 不重置 `ready` 状态的问题：Android 签名 URL 变化时刷新一次新 document，iOS 继续固定使用入口 URL，避免把旧 ready 状态误判为新配置成功。
- 二次修复验证：用户端 `typecheck` 与生产构建通过，构建产物确认包含 canonical 卡片版本、`checkJsApi`、Android URL 变化刷新以及现代/旧版接口互斥分支；公网 `https://dzshop.vip/posts/1?zkShare=20260901-2` 以 iPhone 微信 UA 请求返回 HTTP 200、`text/html`，包含 `og:title`、`og:image` 与 canonical。公司发布 DryRun `20260901-082036-73156` 通过且未连接 SSH、未修改生产；真实发布仍等待本次明确确认，不能将上述代码验证表述为真机问题已经关闭。
- 2026-09-01 用户再次明确确认后完成二次生产发布，发布 ID `20260901-082303-69308`，备份位于 `/root/zhenke/.deploy-backups/20260901-082303-69308`。固定流程执行 `docker compose up -d --build` 且未先 `down`；本次后端镜像命中缓存、后端容器继续运行，Redis 保持原容器健康，只重建 UI 容器。公网健康检查首次即返回 200；独立复核确认三个 Compose 服务最终正常、UI healthy，`/api/health` 为 HTTP 200 且 JSON `UP`，首页引用新 `umi.9a952048.js`，线上分享 chunk 包含 `20260901-2`、`checkJsApi` 和 Android reload 分支；帖子公开卡片 HTML、canonical 与 JPEG 封面均为 200，公众号签名接口 HTTP/业务码均为 200。备份清理后仅保留本次与 `20260901-075947-57628` 两份。生产代码已更新，但微信最终消息类型只能由真实手机执行原生发送验收，不能以接口和构建检查代替。
- 用户随后完成了可区分入口来源的真机对照：从微信聊天记录中的纯文本 `https://dzshop.vip` 打开后继续分享会被微信降级为纯链接；从微信搜索进入同一站点后，原生“发送给朋友”可正常生成卡片。该结果确认当前卡片注册链路本身可用，后续不再为入口场景降级改动 JS-SDK。
- 按最新范围将长期内容分享地址收敛为无时效参数的稳定 ID 路径：甄客帖 `/posts/{id}`、普通商品 `/products/{id}`、甄客验 `/reports/{id}`、甄必享 `/enjoy/{id}`；删除 `zkShare` 诊断/缓存版本参数，非微信系统分享和复制链接也不再沿用当前页面的 query/hash。试用明确保持既有 `/products/{productId}?campaign={campaignId}` 契约，不新增 `/trials/{id}`。公开卡片 HTML 每次实时读取仍在线内容并返回 no-cache 元数据，封面继续使用稳定同源 ID 图片端点，不依赖临时令牌。
- 本轮用户端 `typecheck`、生产构建与 `git diff --check` 已通过；新分享 chunk 为 `8683.4fc3334a.async.js`，构建产物中不存在 `zkShare`。公司发布 DryRun `20260901-085059-72808` 通过，未连接 SSH、未修改生产；真实发布等待用户本次明确确认。
- 2026-09-01 用户明确确认后完成长期链接版本生产发布，发布 ID `20260901-085248-69956`，备份位于 `/root/zhenke/.deploy-backups/20260901-085248-69956`。固定流程执行 `docker compose up -d --build` 且未先 `down`；后端镜像命中缓存并持续运行，Redis 保持健康，只重建 UI。独立复核确认 UI 最终 healthy，公网 `/api/health` 为 HTTP 200 且 JSON `UP`，首页引用 `umi.cf338c28.js`，线上分享 chunk `8683.4fc3334a.async.js` 不含 `zkShare`；`/posts/1`、`/products/3`、`/reports/1`、`/enjoy/1` 均返回 HTTP 200 HTML，`og:url` 分别为其无参数 canonical。备份清理后保留本次与 `20260901-082303-69308` 两份。

## 2026-09-01 商城商品列表独立滚动与分页加载

- 保留当前“左分类、右商品”的页面结构和甄客行视觉，只为 `/mall/products` 增加视口级应用布局：搜索栏固定在商品目录上方，页面文档本身不滚动；左侧分类与右侧商品分别使用独立纵向滚动容器，分类少时保持静止、以后分类增多时只滚动分类栏。
- 商品列表恢复 `main` 已验证的滚动分页方式，每页从 16 条收紧为 12 条；首次请求 `pageNum=1`，右栏接近底部 240px 时才请求下一页。观察器以右侧商品栏为根节点，并使用同步防重入锁、请求版本和商品 ID 去重避免重复页或迟到响应覆盖；搜索或切换分类会把右栏恢复到顶部并从第一页重新查询，底部按钮保留为自动观察不可用时的降级入口。
- 后端与 Mapper 未修改。现有 `/shop/mall/products` 已在 Controller 中把 `pageSize` 限制为最多 24，并在公开商品 Mapper 查询前调用 `PageHelper.startPage`，返回真实 `rows/total`，因此不是把全部商品一次性载入 JVM；本轮不改变后续页继续返回准确总数的既有契约。
- 验证证据：用户端 `npm.cmd run typecheck`、`npm.cmd run build` 与 `git diff --check` 均通过。真实页面在桌面默认视口和 `390x844` 手机视口下均满足 `window.scrollY=0`、文档高度等于视口、无横向溢出，左右栏计算样式均为 `overflow-y:auto`；生产公开数据当前只有 3 件商品，页面正常显示且控制台无错误。
- 为覆盖第二页行为，在本地浏览器中临时拦截只读商品列表响应并提供 25 条合成验收数据，没有写入后端或生产数据。首屏只渲染 12 条；仅将右侧商品栏滚到底部后追加为 24 条，网络记录明确为 `pageNum=1&pageSize=12` 后再请求 `pageNum=2&pageSize=12`，期间 `window.scrollY` 与左分类 `scrollTop` 均保持 0。浏览器、构建和截图临时产物已清理；本轮未执行 SQL、未部署、未提交或推送。

## 2026-09-01 分享深链定位与首页白屏修复

- 根因一是设备定位原本只在首页挂载后启动，公共 `AppChrome` 只读取 `sessionStorage`，所以新用户从甄必享、甄客帖、商品或甄客验分享链接直接进入详情时不会请求定位，顶部必然停留在“选择城市”。定位现已抽为全局 single-flight 能力，由公共页面壳在任意非登录入口缺少位置时启动；首页与详情复用同一个请求，成功后统一保存并广播，顶部立即显示市级名称。
- 根因二是首页原本在 effect 中裸调 `navigator.geolocation.getCurrentPosition`。模拟部分微信/嵌入式 WebView 同步抛 `SecurityError` 后，可稳定复现 React 卸载首页形成白屏；新实现把同步异常、异步拒绝、反向地理编码失败及会话存储异常全部转换为可恢复失败，首页继续显示并允许手动选城。自动定位返回较晚时不会覆盖期间完成的手选城市，定位失败文案也会在城市选择器内显示。
- 分享内容首屏由后端元数据文档通过 `document.write` 注入 SPA。为避免微信 WebView 把该文档及 JS-SDK 状态继续带到首页，从分享内容路径进入的会话点击品牌、首页或城市入口时改为一次干净的文档导航；普通站内会话仍保持 Umi SPA 导航，`sessionStorage` 中的当前定位在同标签导航后继续保留。
- 浏览器回归使用全新 `390x844` 会话：模拟授权定位与反向解析后，直达 `/enjoy/1` 顶部显示“保定”，随后点击首页仍显示“保定”，首页请求明确携带 `city=保定市`；模拟 `getCurrentPosition` 同步抛 `SecurityError` 时，详情与首页均继续渲染，控制台不再出现定位异常或 React 崩溃，首页为 0 error；详情仍有本任务前已存在的 Ant Design `Image mask` 弃用提示。线上公开分享详情与首页分包也做了只读核对，当前均返回 200；未写入业务或生产数据。
- 验证证据：用户端 `npm.cmd run typecheck`、`npm.cmd run build` 与 `git diff --check` 通过。本轮未修改后端、数据库迁移或 YML，未执行 SQL、未部署、未提交或推送。
- 2026-09-01 用户明确确认后，将本轮商城列表与分享深链修复的同一组正式产物发布到公司环境，发布 ID `20260901-101233-79312`，备份位于 `/root/zhenke/.deploy-backups/20260901-101233-79312`。固定流程执行 `docker compose up -d --build` 且未先 `down`，Redis 保持原容器运行；启动期健康检查前 4 次短暂返回 502，第 5 次恢复 HTTP 200。独立复核确认三个远端产物 SHA-256 与本地一致，UI healthy，公网健康 JSON 为 `UP`，首页聚合与商城 `pageNum=1&pageSize=3` 分页接口业务码均为 `200`，用户首页与管理端入口均为 HTTP 200；备份策略删除旧的 `20260901-082303-69308` 并保留最新两份。本次未执行 SQL，也未修改 `.env`、Dockerfile、Compose、Nginx、证书或数据库配置。

## 2026-09-02 微信分享直达的全局定位与详情导航闭环

- 将首页独占的城市选择器抽为公共应用壳能力。甄客帖、甄必享、商品、甄客验及其他正式页面点击顶部城市均在当前页面原地打开定位/手选面板，不再跳回首页；首次懒加载有可见反馈，用户拒绝自动定位后打开面板不会再次自动索权，只有主动点击“重新定位”才重试。
- 微信分享冷启动优先通过已验签的 JS-SDK `getLocation` 获取 WGS84 坐标，普通站内入口优先浏览器定位、微信能力作为技术失败兜底，避免 Android 从首页进入详情时因签名 URL 改变产生不必要整页刷新。微信签名、原生定位、浏览器定位和逆地理解析均有超时/失败收口，single-flight 不会被悬挂请求永久占用；失败后仍可手选城市。
- 城市变更继续使用公共事件同步页头，并让首页、甄客帖列表和甄必享列表按既定城市开关重新加载。定位只负责公共位置能力，没有改变第一版城市内容范围规则。
- 新增统一安全返回：有真实站内前序页时正常后退；分享直达或刷新形成的 `history idx=0` 则分别回到商城、甄客帖、甄必享等确定业务父页，不再无响应或退出项目。详情跳登录再回原页使用仅存在于 SPA history state 的来源标记消除重复详情历史项，避免第一次返回看似失效。
- 商品与甄客验详情恢复手机/小平板底部主导航；商品购买栏和甄客验互动栏在 `390px` 及 `641–900px` 断点都固定在主导航上方。商品加载失败也保留返回商城入口。移除对微信原生工具栏的全局隐藏，保留客户端自身返回路径，并修复详情离开后来源页标题覆盖目标页标题的问题。
- 浏览器真实回归以全新商品直达 `/products/3` 会话覆盖 `390x844` 和 `768x1024`：城市面板在原 URL 打开、手选北京后原页即时更新、直达返回落到 `/mall`、底部导航可切到 `/posts`；购买栏与主导航边界分别为 `719–782 / 782–844` 和 `895–962 / 962–1024`，没有像素重叠。隔离微信 UA/JS-SDK 桩回归实际请求签名接口及 `latitude=39.9042&longitude=116.4074` 的逆地理接口，页头自动显示北京；两组会话控制台均为 0 error。
- 验证证据：用户端 `npm.cmd run typecheck`、`npm.cmd run build` 与 `git diff --check` 通过。本轮未修改后端业务、数据库迁移或 YML，未执行 SQL、未部署、未提交或推送；公众号安全域名、真实授权弹窗及 iOS/Android 微信卡片冷启动仍须部署后真机验收，受控 JS-SDK 桩不能冒充该证据。

## 2026-09-02 详情留白、就地回复与评论分页收口

- 甄必享正文卡片及甄客帖/甄必享评论卡片只在各自容器内取消重复的标题上边距，修复首屏无意义留白，不改变其他页面的通用标题节奏。评论输入框字符计数与发布按钮重新分区，移动端不再出现 `0 / 500` 与按钮重叠；无评论状态缩为评论卡片内的轻量空态。
- 评论区删除“几条公开交流 / 几条一级评论”等统计副标题。顶部输入框只负责发布一级评论；点击任意一级评论或二级回复的“回复”，会在被点击内容正下方展开独立输入框并自动聚焦，取消、切换目标和删除目标时同步清理回复状态，不再把用户带回评论区顶部。移动端打开就地回复时暂时隐藏悬浮发布入口，避免遮挡回复按钮。
- 甄客帖与甄必享沿用真实服务端分页：一级评论每页 10 条；每个一级评论首屏最多附带 3 条回复预览；展开回复时按该一级评论独立请求，每页 10 条，并分别提供“查看更多评论 / 查看更多回复”和剩余条数。回复接口服务端单次最多允许 20 条，避免客户端放大请求；Mapper 继续通过 PageHelper 和单根评论条件分页，不会把整棵评论树一次性返回浏览器。
- 长列表异步状态增加按一级评论区分的请求代次：一级列表刷新、删除或路由离开后，旧回复分页响应不能再覆盖新状态；刚发布且位于末页的回复会与后续分页结果去重合并，不会在首次展开时暂时消失。评论提交期间输入与删除操作互斥，避免慢请求吞掉草稿或同时删除回复目标。
- 回复创建把公开内容、有效目标评论和有效根评论的校验合并到 Mapper 单条 `INSERT ... SELECT`，`parent_comment_id` 由数据库中的真实根评论派生。目标或根评论并发删除时插入返回 0 并提示“回复的评论不存在或已删除”，不会生成仍计数但不可见的幽灵回复；根评论创建路径保持不变，不需要数据库迁移。
- 浏览器在 `390x844` 与 `1280x900` 检查了甄客帖、甄必享的空评论、长评论及嵌套回复：点击靠后的一级评论和二级回复均在原位置显示输入框，页面不回到顶部；请求记录分别为评论 `pageNum=1&pageSize=10`、下一页 `pageNum=2&pageSize=10`，以及单根回复 `/replies?pageNum=1&pageSize=10`。长列表数据只通过本地浏览器请求拦截生成，没有写入后端或生产数据；真实页面仅保留任务前已存在的 Ant Design `Image mask` 弃用提示，未观察到本轮新增运行时错误。
- 验证证据：用户端 `npm.cmd run typecheck` 与生产构建通过；后端 `mvn.cmd -q -f delivery-backend/pom.xml -pl ruoyi-shop -am test` 在正常权限环境执行 138 项测试，通过 138、失败 0、错误 0，包含两套 Service 的并发删除失败分支和两份 Mapper 原子回复 SQL 结构检查；本轮未新增迁移、未执行 SQL，正式发布结果见下一条。
- 2026-09-02 经用户明确确认，重新执行无 Cloud 参数的生产后端完整打包（8 个模块成功、138 项测试全过）及用户端/管理端 typecheck、生产构建后，使用公司固定无 `down` 流程完成发布，发布 ID `20260902-041911-39960`，备份位于 `/root/zhenke/.deploy-backups/20260902-041911-39960`。Compose 仅执行 `docker compose up -d --build`，Redis 保持原容器健康运行；启动阶段前 4 次公网健康检查为短暂 502，第 5 次恢复 HTTP 200。独立复核确认 `zhenke-server` 持续运行、`zhenke-ui` healthy、公网健康 JSON 为 `{"status":"UP"}`，远端 JAR、用户端和管理端 `index.html` 的 SHA-256 均与本地产物一致。备份策略删除旧的 `20260901-085248-69956` 并保留最新两份；本次未执行 SQL，也未修改 `.env`、Dockerfile、Compose、Nginx、证书或数据库配置。代码提交与 PR 分支推送在本条记录之后执行。
