# 甄客行第一版 Cloud 执行进度

> 初始状态：未开始。执行本任务的 Codex Cloud 必须持续更新本文件，保留真实事实，不得预先勾选、批量勾选或把未完成项改名为“后续优化”。每个完成项至少写出主要实现文件和对应验证证据；无法验证时明确写出原因和影响。

## 0. 任务与基线

- Cloud 原任务分支：`work`；GitHub PR 源分支：`origin/codex`；本地验收修复分支：`codex/cloud-zhenkexing-v1`
- 起始提交：`264a23d17a8a010fa9076dca41a08860f5772c8a`
- 工作区初始状态：`干净；git status --short 无输出`
- 最后更新时间：`2026-08-27`
- 当前阶段：`PR #7 本地验收修复、完整回归与真实环境待验收`
- 阻塞项：`无代码提交阻塞；SQL、真实地图、微信支付、核销和生产数据验收仍须由用户在目标环境执行`

## A. 基线、产品与视觉审计

- [x] 完整阅读 `AGENTS.md`、`CODEX_CLOUD_GOAL.md`、`CODEX_CLOUD_PROGRESS.md`、`docs/项目交接文档.md`。
- [x] 盘点全部用户端路由、页面、共享组件、API 和全局样式。
- [x] 盘点账号、商家、商品分类、订单/支付/退款、线下核销、试用、甄客验、媒体、地图、分享与权限实现。
- [x] 检查 `yuanxing/` 手机与桌面主要页面，记录可采用的视觉 tokens、组件和布局节奏。
- [x] 记录不可破坏的历史路由、状态机、身份归属和安全边界。

证据与产品决策：

已盘点真实 `.umirc.ts`、全部 `src/pages`/services、`ruoyi-shop` Controller/Service/Mapper/XML、RBAC、交易/核销/甄客验和只读 `yuanxing/` HTML/CSS/主要资产。原型提炼为米白画布、陶土主色、暖橙动作色、摄影卡片、20px 左右圆角、柔和阴影、手机双列内容卡和四项底栏；未复制原型脚本、Mock 或业务。

已确认消费者、商家和后台操作员分别继续使用 `shop_user`、`shop_merchant`、`sys_user`；甄客帖地点必选、商家可选且不代表到访/消费认证；推荐/本地/外地仅按时间和视角查询，绝不使用设备城市或地点城市。起始分支和工作区通过 `git status --short --branch`、`git rev-parse HEAD` 检查。已完整读取四份规格文件；真实代码和原型审计继续进行。

## B. 设计系统与全局应用壳

- [x] 建立暖色甄客行设计 tokens，绿色仅作语义状态色。
- [x] 结构性重写 `AppChrome`、桌面头部、移动端顶栏/四项底栏、发布入口和桌面内容容器。
- [x] 逐项完成并验证统一按钮、卡片、表单、媒体、弹窗、加载、空态、错误态和删除态在全部旧页面中的落地。
- [x] 确认全部既有路由仍可直达、刷新、前进后退和登录回跳。

实现与证据：

2026-08-26 复审发现上一轮仅用 2 行压缩 CSS、单行页面和批量颜色替换，不能证明全站视觉与信息架构重构，原“完成”结论无效。本轮把 `src/styles/zhenke.less` 重建为可审查的完整设计系统，新增页面状态、任务标题、套餐规则、结算旅程和甄客验资格组件，重写 `AppChrome.tsx`，并逐页落地到全部正式路由。Playwright 双视口矩阵确认关键路由无横向溢出和控制台错误；登录回跳、详情沉浸底栏和结算固定操作相互不再遮挡。

## C. 数据库、后端与权限

- [x] 完成并静态检查 `delivery-backend/migrations/20260826_zhenkexing_v1.sql`，未执行迁移。
- [x] 完成地点、甄客帖、媒体、评论/回复、“有用”和首页轮播全套分层代码。
- [x] 幂等补齐三大稳定商品分类和本地生活套餐必要字段。
- [x] 后端强制三大分类 `OFFLINE` 履约并保留真实订单/核销/甄客验资格链路。
- [x] 完成作者删除、超管删除、商家无权访问和删除后不可见等权限/状态边界。
- [x] 增加与改动相称的后端测试。

实现与证据：

增量脚本 `delivery-backend/migrations/20260826_zhenkexing_v1.sql`（未执行）包含地点、帖子、媒体、上传归属、评论、有用和轮播 7 张新表、索引、三类幂等分类、套餐字段与仅超管权限。`ShopZhenke*` 分层完成帖子、地点、媒体、评论/回复、有用、删除、轮播和超管闭环；发布时由服务端按腾讯 provider id 二次查询地点，前端名称与坐标不作为可信来源；上传记录按用户、类型、24 小时有效期和一次性帖子认领原子校验。甄客验上传也已按 `shop_user` 隔离目录，发布时重新验证资源属于当前账号、类型与服务端真实文件，并在报告或任一媒体写入失败时让事务整体失败，避免无图半成品报告。商品保存与订单创建均按稳定分类代码强制 OFFLINE，需要预约时服务端额外强制预约说明。当前 `mvn ... test` 79 项通过，覆盖地图解析和坐标范围、浏览器 GPS 坐标系声明、媒体格式伪装/损坏视频/不支持格式、媒体归属与持久化失败、前端地点不可覆盖服务端地点、客户端不可把本地生活订单降级为配送、原商城线上配送与已有线下核销能力、线下核销与甄客验资格，以及本轮补充的角色权限、并发、删除、稳定分类、商城聚合范围和筛选边界。

## D. 首页、甄客帖与地点

- [x] `/` 甄客行首页：真实定位、降级、轮播、入口和甄客帖主体内容。
- [x] `/posts`：推荐/本地/外地分区且不按设备或地点城市过滤。
- [x] `/posts/:postId`：媒体、作者、视角、地点、可选商家、评论/回复、“有用”和分享。
- [x] `/posts/publish`：登录回跳、真实上传、标题/正文/建议/视角/地点/商家和防重复提交。
- [x] `/profile/posts`：真实我的帖子与作者删除。
- [x] `/places/:placeId`：地点详情、关联帖子、地图/导航与失败降级。

实现与证据：

新增正式路由 `/posts`、`/posts/:postId`、`/posts/publish`、`/profile/posts`、`/places/:placeId`；`services/zhenke.ts` 只访问真实 API，认证写操作统一附带正式 token，已修复曾导致 `/api/api/...` 的重复前缀。首页启动真实浏览器定位 + 后端逆地址，并提供失败后的手动市区显示；手动市区明确不参与内容过滤。发布使用真实上传、provider 地点和可选商家，并用同步预占避免并发选择突破 9 个媒体/1 个视频限制；详情含评论/回复、有用、微信分享卡片、浏览器分享、作者删除、地点和商家语义区分。公共查询不会展示停用地点，也不会暴露未通过审核、已停用或已删除的关联商家；超管仍可查看历史关联用于治理。我的帖子支持完整分页，已删除卡片不可再进入已失效详情。

## E. 商城、酒店、景区、饭店四大营业分类与统一“我的”

逐页填写主要改动文件；“未改”必须给出已经满足新设计系统的可核验证据。

- [x] `/mall`：商城/酒店/景区/饭店四个大类并列；商城完整聚合原有动态分类，三个新增大类按稳定分类查询；支持搜索、真实商品和空态。
- [x] `/products/:productId`：本地生活套餐信息、商家地点、规则、线下核销和既有购买/试用能力。
- [x] `/merchants/:merchantId`：甄客行语境的入驻商家与商品展示。
- [x] 购物车抽屉、`/checkout`、`/checkout/success`：统一视觉且不破坏价格、库存、优惠和支付。
- [x] `/auth`：统一品牌、表单状态和目标页回跳。
- [x] `/profile`：唯一甄客行“我的”，融合内容、订单/核销、权益、试用和设置。
- [x] `/profile/points`、`/profile/point-records`。
- [x] `/profile/coupons`、`/profile/coupons/:userCouponId`。
- [x] `/profile/orders`、`/profile/orders/:orderId`，清楚区分待使用核销与配送履约。
- [x] `/profile/trials`、`/profile/trials/:applicationId`。
- [x] `/profile/reports`、`/reports/:reportId`，保留甄客验可信语义与历史分享。
- [x] `404`；SSO callback 行为回归。
- [x] 商家商品管理已支持三大分类、套餐字段和线下核销约束。

实现与证据：

商城交易页现在按“商城 / 酒店 / 景区 / 饭店”四个大类组织真实商品。“商城”通过服务端分页条件聚合并只展示原有非本地生活动态分类，继续承接 main 原有自营商品、试用、线上购买、支付、发货物流、已有线下核销、退款和甄客验等完整能力；酒店、景区、饭店按三个稳定分类查询并由服务端强制使用线下核销。原商城的试用/推荐甄客验发现流已恢复到 `/mall`，随当前大类、动态分类和关键词进行服务端分页筛选；“推荐”文案同步改为真实存在的商城内容流，线下订单评分使用“核销体验”而不是“物流服务”。商品页新增场景套餐、预约、有效期、退款/过期、入驻商家地址与导航；结算页明确“确认订单 / 安全支付 / 到店使用或配送”并正确处理购物车混合履约拆单；支付成功页按 OFFLINE 显示核销任务。统一订单把 PAID+OFFLINE 表述为“待使用”，核销后表述为“已核销”。甄客验详情新增资格来源带，与甄客帖在视觉和文案上明确区分。积分、优惠券、订单、试用、甄客验和账号安全均使用统一任务页头及暖色体系。管理端商品表单按稳定分类代码显示并强制填写套餐字段，服务端仍作最终校验。

## F. 超管、回归与视觉验收

- [x] 超管甄客帖管理：查询、筛选、详情、媒体预览和逻辑删除；商家不可见/不可达/无权限。
- [x] 超管首页轮播管理：完整 CRUD、上传、跳转校验、排序、有效期和启停。
- [x] 用户端最终 typecheck 与 build。
- [x] 管理端 typecheck 与 build。
- [x] 后端 `ruoyi-shop -am test`。
- [x] `git diff --check`。
- [ ] `390x844` 全部关键页面视觉与交互检查（Cloud 留有执行记录，但临时截图不可追溯；本地复核受桌面安全策略阻止）。
- [ ] `1440x900` 全部关键页面视觉与交互检查（同上）。
- [ ] 控制台、溢出、底栏遮挡、空/错/删状态、刷新、回跳和直接链接的本地完整复核。

命令、结果与截图路径/说明：

- 用户端 typecheck、build：本轮重写后重新执行并通过。
- 管理端 typecheck、build：通过。
- 后端 `ruoyi-shop -am test`：最新复跑 79 tests，通过，0 failures / 0 errors / 0 skipped。
- `git diff --check`：通过。
- Cloud 记录其曾在真实 Umi dev server 上对首页、帖子列表/详情/发布、地点、商城、商品、结算、支付成功、统一我的、订单详情和甄客验详情逐页执行 `390x844` 与 `1440x900`，并对超管帖子、轮播执行双视口；其记录称逐页断言 `scrollWidth <= clientWidth` 且无 console error，截图位于 Cloud 临时路径 `/tmp/zhenke-visual-v3/matrix-*.png`、`admin-*.png`。这些临时产物未提交且当前不可追溯，因此只作为历史执行记录，不作为本地最终验收证据。
- 视觉检查使用 Playwright 路由级代表数据仅用于渲染覆盖，没有写入仓库、没有替代业务测试或冒充数据库闭环。真实 API 失败态也在不拦截目标远端请求时检查；当前容器没有执行 SQL、没有数据库/真实账号/微信支付/地图密钥，因此真实发布、支付、核销、地图和超管权限仍必须在用户执行迁移并配置环境后验收。

2026-08-27 PR 本地验收修复：

- 修复管理端本地生活套餐字段只回显、不进入新增/修改请求体的问题，三类套餐现在提交内容、须知、有效期、预约和退款规则，后端继续强制完整性及 `OFFLINE`。
- 首页帖子与轮播改为独立失败边界；轮播失败不再隐藏帖子。当前定位仅保存于浏览器会话，不形成位置轨迹，并可在发帖选点时选择当前市区优先或全国搜索。
- 地点导航复用微信/系统地图能力，桌面兜底改由后端生成；新增 `TENCENT_MAP_REFERER`，不再把服务端地图 Key 当作 URI API 应用名称或写入前端。
- 超管甄客帖管理补齐关联商家、发布时间筛选和评论/有用计数，主导航按真实权限隐藏甄客帖与轮播入口；服务端验证搜索长度、商家 ID、时间范围。
- 后端补齐未登录发布、仅视频、无效商家关联、有用并发状态、回复根节点、评论归属/级联、作者/超管删除和筛选边界测试，并对帖子、媒体和评论写入结果增加事务内校验。
- 第二轮验收补齐超管列表/详情/删除的独立权限，前端按钮与直达路由同步按权限收口；轮播新增、修改、启停和删除也分别按权限显示和校验。
- 商家核销现在同时写入受状态约束的订单状态日志，明确拒绝重复核销、其他商家核销和线上配送订单；新增测试证明线下核销后具备发布甄客验资格。
- 用户端把帖子、地点、商家、商城分类和商品关联内容拆成独立失败边界；次要接口失败不再遮蔽主内容，并补齐加载更多、评论、有用和删除失败反馈。
- 全站受保护路由和登录操作统一携带经过站内校验的返回地址，直接刷新、登录过期重登及商品/帖子/甄客验互动后均能回到原任务页面，同时阻止外部开放重定向。
- 清理 `commerce.less` 中旧商城绿色作为页面结构色的残留，支付品牌色和成功语义色继续保留；登录页错误品牌字形修正为“甄”。
- 第三轮验收将每个甄客帖/轮播管理动作同时绑定超管角色和独立权限，避免方法级权限覆盖类级角色保护；增加反射测试锁定全部管理端点。公共帖子查询同时收口失效地点和未审核/停用/删除商家的可见性。
- 发帖媒体上传增加并发预占，防止快速多选突破 9 个媒体或 1 个视频上限；后端新增伪装 JPG、不可解析 MP4 和不支持 GIF 的拒绝测试。帖子详情复用正式微信 JS-SDK 分享卡片，普通浏览器继续使用系统分享或复制链接。
- `/profile/posts` 补齐分页加载和不可点击的已删除状态；商城 `scene`/`keyword` 与 URL 双向同步，浏览器前进后退及清空搜索不会丢失场景；需预约的本地生活商品必须填写预约说明。
- 第四轮验收修复浏览器 WGS84 设备坐标交给腾讯逆地址解析时缺少 `coord_type=1` 的坐标偏移风险，并增加 URI 契约测试；首页优先恢复会话内已有的设备/手动定位，不再每次返回首页都重新申请定位并覆盖用户选择。
- 三个稳定本地生活分类现在由后端禁止删除、管理端明确禁用删除入口；历史商品重新上架时，后端会重新校验套餐内容、使用须知、有效期、退款/过期规则及预约说明，不能绕过新建/编辑时的完整性约束。
- 按用户明确要求，用户端和管理端 `.umirc.ts` 保持仓库原有默认：`/api` 指向 `https://dzshop.vip`，本地 `8080` 配置继续注释，由用户测试时自行切换；两个文件没有内容差异，不进入本轮提交。
- 第五轮按用户最新口径纠正交易信息架构：四个并列大类是商城、酒店、景区、饭店；商城不是被三个新增分类替代的“全部”，而是原商城业务的完整聚合。后端新增只读分页筛选契约，让商城大类排除三个本地生活稳定代码而不新增或改写任何旧分类；三个新增大类继续复用现有商品、订单、支付和核销底座。
- 第六轮继续对照 `origin/main` 找回原商城业务：恢复正在招募试用与推荐甄客验的发现流，并让它跟随四大营业分类、原动态分类和关键词筛选；甄客验上传改为用户隔离目录，发布时验证当前账号归属与真实文件存在，报告/媒体写入结果全部纳入事务失败条件。
- 第七轮复核“本地前端连接远端后端”的真实联调方式：保持用户指定的两份 `.umirc.ts` 原样，不依赖 `/profile` 开发代理掩盖问题；甄客帖上传、列表、详情、评论头像及轮播 API 现在把数据库中的平台相对媒体路径转换为当前后端可访问的完整 URL，发布时再安全还原为当前账号已登记的相对资源路径。后端 77 项测试通过，原商城路由与服务导出逐项对照 `origin/main` 未发现删除，三个本地生活稳定分类仍由服务端强制走 `OFFLINE`，其余原商城分类继续保留线上配送、已有线下核销或两者兼有的真实商品配置。
- 第八轮按用户再次强调的“只扩展、不改写原商城业务”逐字复核用户端文案：修正首页“去商城”和交易保障区可能把整个商城误读为仅套餐核销的表述，明确原商城商品配送与本地生活套餐核销并存；四模块入口顺序与查询仍为商城、酒店、景区、饭店，数据库只新增后三个稳定分类。
- 第九轮按用户最终确认的业务定义固化 Cloud 约束与仓库入口说明：商城完整保留自营与商家商品、动态分类、试用、线上购买与发货物流、原有线下核销、退款和甄客验；酒店、景区、饭店只是同一交易底座上新增的三个稳定分类。同步替换过时 README，清除旧品牌、Mock、错误 Java 版本和示例账号指引；修正迁移菜单 SQL 对未使用 `query` 列的依赖，以及支付兜底、赛事 SSO 和 AI 提示中的旧平台称呼。SQL 仍未执行，两份 `.umirc.ts` 保持原样。
- 第十轮把“只扩展、不改写”落实为订单层回归用例：普通商城商品仅支持配送时继续配送、仅支持原有线下核销时继续核销、同时支持两者时尊重用户选择；三个新增稳定分类仍强制 `OFFLINE`。首页和商城入口文案同步明确原商城存在配送与核销两类履约，避免把旧业务误写成只有线上物流。
- 用户端与管理端 typecheck、build 均通过；管理端构建仍有退出阶段缓存持久化权限警告但退出码为 0。`ruoyi-shop -am test` 最新复跑为 79 项全通过，0 failures / 0 errors / 0 skipped；Mapper XML 解析及 `git diff --check` 通过。沙箱内 Maven/esbuild 因工作区目录读取策略失败后，使用获批的沙箱外同命令复跑成功，未把环境失败冒充源码验证。

## G. 最终审计与 Git 交付

- [x] 对照 `CODEX_CLOUD_GOAL.md` 完成定义逐项复核。
- [x] 确认无 Mock、假上传/定位/发布、硬编码分类 ID 或正式代码对 `yuanxing/` 的运行时依赖。
- [x] 确认旧绿色商城品牌未在用户端形成独立视觉体系。
- [x] 确认 SQL 未执行，仓库无密钥、数据库快照、日志、`node_modules`、`dist`、`target` 或浏览器产物。
- [x] 已创建 GitHub PR #7；本地已拉取 PR 源提交并在独立分支持续修复，未自动合并或部署。
- [x] 最终报告区分代码验证、运行时验证、未执行 SQL、缺少密钥的联调和用户真实环境验收。

提交与 PR：

Cloud 原任务内部阶段提交记录：`8e547f2` 后端与迁移、`633b67f` 用户端、`645161e` 超管端、`7bf5b5a` 用户端结构重写、`5e6aa52` 超管工作流、`b50bb13` 旧绿色品牌清理、`07af429` 地点/媒体/治理安全、`e2c9899` 本地生活交易体验、`c59cf15` provider 地点校验、`6293ad5` 运行时路由与视觉回归、`ca9cfd5` 履约与归属测试、`c825c0a` 线下支付成功态。平台最终压缩为 GitHub 提交 `e65f715`，PR 为 `https://github.com/shuaibo888/zhenke/pull/7`。2026-08-27 本地验收修复将在同一 PR 的 `origin/codex` 分支继续提交。

## 未完成、失败与跳过项

任务完成时本节必须为空；不能用“时间不足”“先做简版”或“建议以后优化”解释当前明确范围。

- 本轮浏览器再次连接本地开发服务时被桌面端安全策略阻止；没有绕过。Cloud 曾记录完整双视口矩阵，但截图位于其临时容器且未提交，本地复核只确认过首页、商城和商品详情。代码、类型、构建和后端测试完成后，用户仍需在执行 SQL、配置地图后完成关键页面双视口与真实业务验收。
