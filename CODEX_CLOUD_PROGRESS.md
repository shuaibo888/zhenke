# 甄客行当前交付状态

> 更新时间：2026-09-02
>
> 状态：第一版、此前体验修复与 2026-09-02 社区互动增量均已发布到公司生产环境。社区互动增量完成代码实现、类型检查、生产构建、后端测试和生产健康核对，当前无已知代码阻塞；生产发布 ID 为 `20260902-110521-45808`。历史阶段与旧命令流水以 Git/PR 记录为准，不再在本文件重复维护。

## 1. 当前基线

- 本地工作分支：`codex/cloud-zhenkexing-v1`。
- GitHub PR：[#7](https://github.com/shuaibo888/zhenke/pull/7)，真实源分支为 `origin/codex`。
- `main` 不是本轮交付目标，不自动合并或推送。
- `delivery-backend/migrations/20260902_zhenkexing_community_v2.sql` 已由用户在发布前手动执行；本次 Codex 流程未直接连接数据库或重复执行迁移。

## 2. 当前已交付范围

- 首页以真实定位、运营轮播、甄客帖故事流和纵向甄必享为主体；定位失败可手选城市，城市范围服从后端 `shop.zhenkexing.cityFilterEnabled`。
- 甄客帖已具备真实地点、可选商家、三种内容视角、媒体上传、详情、评论/回复分页、“有用”、分享、作者删除和超管治理闭环。
- 甄必享是独立的超管官方地点专题，按“玩 / 吃 / 住 / 购”展示；首页每类 2 条，“查看更多”使用完整分页，普通用户不能投稿或归类甄客帖。
- 交易侧并列呈现商城、酒店、景区、饭店。原商城的商品、试用、购物车、支付、配送、原有核销、退款和甄客验完整保留；后三类复用统一订单体系并由服务端强制线下核销。
- 唯一 `/profile` 已整合资料、我的甄客帖、甄客验、消息中心、订单、积分、优惠券、试用、地址和账号安全。
- 甄客帖与甄客验的“有用”、评论和回复会生成持久化站内信；收件人由服务端推导，自互动不提醒，稳定去重。当前超管甄必享不发送站内信。
- 甄客帖和甄客验的消费端互动统一表达为“有用”，作者本人不可操作；甄必享保留原有 like 数据与接口，但消费端和运营端统一表达为“喜欢”，两类数据不混算。
- 甄客帖新增人工“编辑推荐”：后台独立权限可设为或取消精选，首页最多展示 3 条、空时隐藏、服从城市范围，并与下方普通帖子轨道去重；普通帖子列表仍按发布时间与三种内容视角浏览。
- `/profile` 新增“我的内容累计获得 X 次有用”，可见区分甄客帖与甄客验计数，并可进入 `/profile/useful` 查看两类分页明细；甄必享喜欢不计入。
- 帖子入选编辑推荐会产生无消费者发送者的系统消息；后台操作员身份与内部精选版本不通过公共帖子 JSON 暴露。
- 超管端已提供甄客帖、甄必享和首页轮播管理，并保持商家角色不可见、接口无权限。
- 用户端保持统一白底、暖橙操作、深色文字和摄影内容的甄客行视觉；旧绿色只保留为成功等语义状态。

## 3. 最近关键交付

- 分享直达已经具备全局定位、当前页选城和安全业务返回；商品与甄客验详情的操作栏、底部导航在手机和平板断点不重叠。
- 甄客帖与甄必享评论支持就地回复、一级评论分页和单根回复分页；并发删除或迟到响应不会留下错误回复状态。
- 甄必享“查看更多”统一为玩、吃、住、购顺序；“我的”已压缩首屏信息密度并增加消息中心及未读角标。
- 悬浮“发布甄客帖”只在首页和甄客帖主列表展示；个人中心、交易、官方内容、详情、表单和异常页不展示。
- 积分明细按入口来源返回：从“我的”进入返回 `/profile`，从积分中心进入返回 `/profile/points`，无来源直达默认返回积分中心。

## 4. 验证与发布状态

- 当前增量后端完成编译及完整 `ruoyi-shop` 测试，共 162 项，通过 162、失败 0、错误 0；新增覆盖精选状态、首页聚合、系统通知、个人累计/分页、权限和三个变更 Mapper 的 MyBatis DTD 解析。
- 用户端与管理端均已通过 TypeScript 检查和生产构建；最终差异通过 `git diff --check`。沙箱内 Umi 构建因父目录读取权限失败，授权环境重跑成功，属于验证环境限制而非源码构建失败。
- 已在 `390×844` 的本地真实浏览器中，用内存测试桩核对首页精选去重、个人帮助汇总、帮助明细和甄客验“有用”详情；在 `375×812` 复测四页页面宽度均等于视口宽度，底部操作栏与空商品封面降级正常，补齐测试桩后控制台为 0 error。测试未连接真实后端、数据库或真实账号，也未进行真机业务验收。
- Playwright 在 `390×844` 下验证两条积分返回链、无来源直达，以及 14 个代表路由的悬浮发布白名单；个人中心底部无按钮遮挡，控制台 0 error。登录态数据使用本地只读测试桩，没有读取或写入真实用户数据。
- 首次生产发布暴露 `ShopTrialMapper.xml` 与 `ShopZhenkeMapper.xml` 的 `resultMap` 子元素顺序不符合 MyBatis DTD；发布脚本均自动回滚。修复两处顺序并增加三个变更 Mapper 的 DTD 回归测试后，最终发布 `20260902-110521-45808` 在第 5 次健康检查返回 HTTP 200；`zhenke-server`、`zhenke-ui`、`zhenke-redis` 正常，公网 `/api/health` 返回 `{"status":"UP"}`，`/api/shop/zhenke/home` 返回业务 `code=200`。

## 5. 仍有效的边界与后续方向

- 当前无已知代码待办；社区互动增量已完成迁移前置与公司生产发布，仍建议补充真实账号下的互动闭环和真机视觉验收。
- 甄必享仍是超管官方内容。未来如改为优质用户帖榜单，需先定义周期、城市与品类、有效互动、反作弊、内容安全、申诉和人工运营规则。
- 城市开关关闭时公共帖子与甄必享展示全平台内容；开启后按当前定位或手选城市过滤，缺少城市不得静默回退全平台。内容视角与城市范围始终是两个独立条件。
- 收藏、关注、评分、推荐算法、垂直预订、多租户和赛事内建不属于当前已确认范围。
- 当前消息功能依赖目标库已存在 `shop_notification` 表；后续数据库变化必须新建未使用过的版本化增量脚本，不恢复或重跑已应用脚本。
