# 㤫者商城用户端确认稿实施计划

> **执行要求：** 本计划在当前 `main` 工作区分阶段执行，不创建分支，不使用子代理。每个阶段最多修改 3 个文件，先写失败测试，再实现并验证。

**目标：** 以 `/Users/zhangruoli/Downloads/㤫者商城_前端预览图_确认版` 中四张确认图为唯一视觉标准，在不改变后端接口、字段、状态和权限的前提下重构用户端页面。

**架构：** 保留现有 React/Umi 单页业务和真实接口，先通过结构测试锁定关键产品规则，再逐步调整 `index.tsx` 与 `index.less`。普通订单和试用申请只统一视觉，不合并数据源、状态或操作接口。

**技术栈：** React 19、Umi 4、TypeScript、Ant Design 6、Less、Node Test Runner。

---

## 阶段一：首页与全局移动导航 ✅（Claude 续作，2026-07-23）

**文件：**
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [x] 移动端首页由 `column-count` 瀑布流改为等高对齐 CSS Grid（两列），根治高低错落。
- [x] 卡片统一 1px 清晰边框、圆角、图片 1:1 比例，图片下沿与底部数据行加分隔线，卡片等高。
- [x] 品牌 logo 改为居中绿色文字（#087246）。
- [x] 顶部 header、分类栏加底部分隔线；分类栏选中态加绿色下划线；收紧留白。
- [x] 保留底部“首页/我的”与右下角悬浮购物车。
- [x] 新增本地预览深链 `?preview=1&view=<destination>`，便于逐页截图与手机预览。
- 说明：无头截图工具视口存在裁切 bug（分类栏第4项/详情页评分第3项被裁），已由真机确认 DOM 与真实布局正常。

## 阶段二：甄客验详情 ✅（Claude 续作，2026-07-23）

**文件：**
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [x] 去掉来源标签（不再显示“购买评价/甄客”来源 Tag）。
- [x] 有用图标由红心 `HeartOutlined` 改为绿色点赞 `LikeOutlined/LikeFilled`。
- [x] 顶部栏改为：返回 + 作者头像与昵称 + 分享。
- [x] 三项评分压成一行（大数字 + 小标签 + 竖分隔线），仅 `reportSource === 'PURCHASE'` 渲染；试用甄客验不显示评分，不计算综合分。
- [x] 新增“真实体验”小标题、关联商品卡片（图 + 价格 + 查看商品）、底部固定操作栏（有用/评论/立即购买）。
- 待办：结构测试断言与 `npm test`/`npm run build` 需在获授权后补跑。

## 阶段三：试用招募详情

**文件：**
- 修改：`delivery-frontend/src/pages/index.structure.test.ts`
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [ ] 新增失败测试，锁定目标人数、已领取、剩余人数、截止日期和只读试用方式。
- [ ] 按第二版确认图重排试用招募详情，保留“直接购买/申请试用”。
- [ ] 线上流程只显示申请、审核、发货、确认收货、发布甄客验；线下流程不显示物流步骤。
- [ ] 不加入三天倒计时、处罚、配送方式选择或其他未支持能力。
- [ ] 运行结构测试、类型检查和构建。

## 阶段四：个人中心骨架 ✅（Claude 续作，2026-07-23）

**文件：**
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [x] 首屏改为：身份卡（头像+昵称+编辑资料/收货地址）→ 绿色“我的甄客验”横条（待发布/已发布/全部）→ 我的订单筛选。
- [x] 移除菜单式“我的服务”与独立“我的试用”入口。
- [x] 订单筛选顺序固定为：全部订单、待付款、待发货、待收货、待发布、售后（`orderStatusFilter` 状态）。
- [x] 点击筛选进入订单列表并按状态过滤；保留底部“首页/我的”与悬浮购物车。
- 待办：结构测试断言与 `npm test`/`npm run build` 需在获授权后补跑。

## 阶段五：统一订单卡片与订单详情

### 子任务五-A：统一订单列表

**文件：**
- 修改：`delivery-frontend/src/pages/index.structure.test.ts`
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [ ] 为购买订单、线上试用、线下试用建立统一的前端展示模型，但保留原始对象和各自操作回调。
- [ ] 按真实状态映射到订单筛选，不把试用申请伪造成订单。
- [ ] 验证线上试用物流与确认收货、线下试用待发布操作仍走原接口。

### 子任务五-B：订单详情

**文件：**
- 修改：`delivery-frontend/src/pages/index.structure.test.ts`
- 修改：`delivery-frontend/src/pages/index.tsx`
- 修改：`delivery-frontend/src/pages/index.less`

- [ ] 使用现有订单详情数据增加移动端订单详情视图。
- [ ] 展示商品、金额、地址、时间、物流、退款和状态记录。
- [ ] 复用现有支付、取消、确认收货、退款及物流操作。

## 阶段六：全流程验证

**文件：**
- 修改范围在执行前根据失败用例单独拆分，每个子任务不超过 3 个文件。

- [ ] 运行 `npm test`。
- [ ] 运行 `npm run build`。
- [ ] 检查首页全部内容与四分类切换。
- [ ] 检查甄客验详情的购买/试用评分差异。
- [ ] 检查线上、线下试用流程差异。
- [ ] 检查普通订单与试用申请的数据源和操作没有串用。
- [ ] 检查微信内限制、购物车、地址、支付、物流、退款和发布甄客验的现有能力未回归。
- [ ] 列出潜在问题及对应测试用例。


---

## 追加优化（Claude，2026-07-23 下午）

针对产品经理真机反馈的三点整改：

1. **甄客验详情顶图改为甄客实拍图**：`renderReportDetail` 顶部改为“甄客实拍”主图 + 多图缩略条（小红书式），加 `甄客实拍` 角标；关联商品卡单独展示商品图，与实拍图区分。preview 报告 201 的 `resources` 换成与商品封面不同的素材以便肉眼可辨。仅改前端展示与 preview 假数据，不动 `resourceUrl` 真实映射逻辑。
2. **个人中心“我的订单”版式对齐样图**：首屏改为身份卡 + 绿色“我的甄客验”横条 + 订单筛选 tab；点击 tab 进入 `orders` 视图并按 `orderStatusFilter` 过滤。
3. **订单/试用/甄客验列表卡片改为电商风（京东/拼多多/小红书）**：
   - 订单卡 `.orderCard`：商家头像+店名+状态行 / 商品图+标题+订单号+价格件数 / 物流·倒计时 / 操作按钮组。
   - 试用卡复用 `.orderCard` 结构：商家、商品图、类型、截止、操作。
   - 我的甄客验 `.reportListCard`：实拍缩略图 + 标题 + 体验摘要 + 类型标签 + 点赞数。
   - 所有卡片统一白底、1px 边框、圆角、分隔线。

未回归项（保持不变）：订单/试用/退款/物流/发布甄客验的真实接口与操作回调、状态映射、微信内限制。

待办：结构测试断言、`npm test`、`npm run build` 需在获授权后补跑；生产部署（打包挂公网）待用户确认后进行。

## 产品经理全量走查与完善（Claude，2026-07-23 傍晚）

系统走查全部页面后修复的缺页与体验问题：

1. **缺页修复：试用申请无操作入口**。个人中心取消独立“我的试用”后，线上试用确认收货、线下试用发布甄客验一度无入口。现按计划阶段五把试用申请并入“我的订单”统一列表：`filteredTrials` 按状态映射到待发货(approved)/待收货(shipped)/待发布(pending_report)，复用 `.orderCard` 结构，保留各自真实操作回调（查看物流/确认收货/发布甄客验）。
2. **个人中心首屏饱满化**。原“点筛选 tab → 跳二级页 → 才有内容”改为首屏内联展示完整订单+试用列表（`renderOrderCards()` / `renderOrderFilterTabs()` 抽为局部函数，menu 与 orders 视图复用），与京东/拼多多“我的”一屏看全一致。
3. **预览态红色“请求失败”报错条**。详情页评论加载未做 preview guard，在预览环境弹错。现 preview 模式直接注入 `previewComments`，评论区展示真实评论线程（提问→作者回复→追评），观感完整。
4. **二级页去除冗余全局 header**。详情页/试用详情页（`journeyView === 'report' | 'product'`）隐藏顶部“㤫者商城”masthead，仅保留页面自身返回栏，聚焦如原生 App。
5. **评论区本土化**。去掉英文 eyebrow “Discussion”，改为“全部评论 N”。
6. **预览支付倒计时**。原 `paymentExpiresAt: 2099` 导致倒计时显示天文数字，改为加载时 +25 分钟的合理值。

未回归项（保持不变）：真实订单/试用/退款/物流/发布接口与操作回调、状态机、微信内限制、软删除评论规则。
待办：结构测试断言、`npm test`、`npm run build`、生产部署需获授权后进行。
